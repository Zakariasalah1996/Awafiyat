# Project TODO

- [x] فحص مستودع `Zakariasalah1996/Awafiyat` وتأكيد أن المصدر الحالي يطابق الإصدار `6d43b643`.
- [x] التحقق من أن مستودع `Zakariasalah1996/expo` هو مصدر إطار Expo وليس كود تطبيق ألف عافيات.
- [x] إنشاء وثيقة تصميم مطابقة لشاشات التطبيق الفعلية واتجاه RTL والاستخدام العمودي بيد واحدة.
- [x] نقل كود تطبيق ألف عافيات وملفاته الأصلية من الإصدار `6d43b643` إلى بطاقة المشروع.
- [x] الحفاظ على اسم التطبيق والحزمة `io.awafiyat.health` ومعرّف مشروع EAS الحالي وإعدادات Firebase.
- [x] ضبط AdMob App ID على المعرّف المعتمد للمشروع مع إبقاء Rewarded Ad في وضع الاختبار مؤقتًا حتى يؤكد المستخدم ظهوره.
- [x] الحفاظ على معرّف Rewarded Unit الحقيقي داخل الإعدادات ليُفعّل بعد نجاح اختبار الإعلان.
- [x] التحقق من أن ملف `eas.json` ينتج APK في ملفي `preview` و`production`.
- [x] تثبيت التبعيات وتشغيل فحص TypeScript والاختبارات المناسبة دون تشغيل ميزات الهاتف عبر المتصفح.
- [x] مراجعة سجل الخادم بعد النقل؛ لا توجد أخطاء TypeScript أو تجميع، وأخطاء قاعدة بيانات المعاينة المحلية لا تؤثر في APK الذي يستخدم `https://alfafiyat.com`، وقد نجح مسار `/api/health` الإنتاجي.
- [x] تحديث هذه القائمة بعلامات الإنجاز قبل حفظ النسخة.
- [x] تجهيز نقطة إصدار واحدة وبطاقة التطبيق للتسليم مع إرشادات بناء APK من زر النشر.
- [x] تأكيد اسم حزمة Android `io.awafiyat.health` والخادم الإنتاجي `https://alfafiyat.com` المستخدم داخل APK وتوثيقهما للمستخدم.
- [x] تتبع طلب تسجيل Push Token إلى المسار `/api/user/push-token` وتحديد أن الخطأ `500` يصدر من طبقة MySQL غير المتوافقة مع PostgreSQL على Render.
- [x] مقارنة مخطط جدول رموز الإشعارات في Drizzle مع قاعدة PostgreSQL الإنتاجية على Render وتحديد عدم التطابق.
- [x] إضافة مخزن PostgreSQL معزول لرموز Expo/FCM وسجل إشعارات لوحة التحكم مع upsert وتهيئة ذاتية غير مدمرة.
- [x] التحقق من إرسال الإشعارات من لوحة التحكم إلى التطبيق؛ نجح الخادم في 4/4 وأكد المستخدم وصولها، مع أولوية Android العادية وعدم طلب Full Screen Intent.
- [x] تحويل Rewarded Ad إلى الوحدة الحقيقية في APK الإنتاجي مع إبقاء `TestIds.REWARDED` لوضع التطوير فقط.
- [x] إضافة اختبارات تمنع تكرار خطأ تسجيل Push Token وتشغيل فحوص TypeScript والخادم وحزمة Android 1.0.78 بنجاح.
- [x] تجهيز قائمة العمل وبطاقة الإصدار المحدثة ونتيجة تشخيص الخادم للحفظ والتسليم قبل النشر.
- [x] تتبع رفع صورة الوصفة من لوحة التحكم وتحديد أن `recipeId` لم يكن يُرسل مع طلب الرفع الأول.
- [x] مقارنة استجابة API بالبيانات المحلية وتحديد أن الخادم كان يرسل صور جميع الوصفات كأنها تعديلات إدارية، مع صحة المطابقة بواسطة `recipeId`.
- [x] تحديد أن كاش `recipe_custom_images_v4` كان يحتفظ بالخريطة المختلطة ويمنحها الأولوية على الصور المحلية.
- [x] إنشاء مخزن PostgreSQL مستقل وربط الرفع المباشر بـ `recipeId` وإرجاع التعديلات فقط مع كاش v5 يمسح الخريطة القديمة.
- [x] إضافة ثلاثة اختبارات لثبات الربط بين `recipeId` و`imageUrl` وعقد API والكاش؛ نجحت ضمن 27 اختبارًا.
- [x] إعادة التحقق محليًا من صور الوصفات مع إصلاح الإشعارات عبر TypeScript و27 اختبارًا وتجميع الخادم وحزمة Android قبل طلب الإذن النهائي.
- [x] دفع الالتزام `ddf3557` إلى فرع `main` وتشغيل نشر Render بنجاح.
- [x] التحقق إنتاجيًا من أن `/api/recipes/images` يعيد أربع صور مخصصة فقط بدل خريطة جميع الوصفات، وأن مسار Push Token الجديد يرفض الرموز غير الصالحة قبل الكتابة.
- [x] تأكيد المستخدم أن صور الوصفات المعدلة من لوحة التحكم وصلت إلى التطبيق؛ إغلاق عطل الصور وعدم تغيير مساره مجددًا.
- [x] التحقق من وجود Push Token مسجل فعليًا في PostgreSQL؛ تأكد إنتاجيًا وجود رمزي Expo ورمزي FCM فعّالين على Android.
- [x] فحص تذاكر Expo وإيصالاتها وتسجيل رموز الأخطاء المنقحة وتعطيل `DeviceNotRegistered`، بدل احتساب قبول HTTP تسليمًا ناجحًا مباشرة.
- [x] إعداد واعتماد FCM v1 في مشروع EAS `ac8a9414-c049-43f6-aa26-4647b61e4d28` والتحقق من توافقه مع `io.awafiyat.health` ونجاح تذاكر Expo.
- [x] إضافة تشخيص مرئي عربي لخطأ Rewarded Ad مع رمز منقح وتصنيف `No fill` أو الشبكة أو خطأ الإعداد وخيار إعادة المحاولة.
- [x] التحقق من AdMob: التطبيق جاهز بلا مخالفات، الوحدة الحقيقية والحزمة صحيحتان، واكتشاف وتصحيح Android App ID الخاطئ مع إبقاء Test ID للتطوير والتشخيص الصامت فقط.
- [ ] إنشاء APK إصلاحي بعد نجاح اختبارات الإشعارات والإعلان وتسليم بطاقة جديدة للمستخدم.
- [x] رفع اعتماد **FCM V1 service account** لمشروع Firebase `awafiyat` إلى EAS وRender كمتغير آمن، مع حذف النسخ المؤقتة وعدم إضافته إلى GitHub.
- [x] اختبار رموز Expo وFCM المسجلة بعد إصلاح الاعتماد؛ نجح receipt من Expo ثم نجح اختبار الإنتاج النهائي 4/4 وأكد المستخدم وصول الإشعارات.
- [x] إضافة اختبار تحكم صامت بـRewarded Test ID يميّز سلامة SDK عن فشل الوحدة الحقيقية ويلتقط رمز ورسالة الخطأ بعد تنقيحهما.
- [x] إعادة تصميم نتيجة `showRewardedAd` لتفريق المكافأة والإغلاق وعدم التوفر، مع مهلات وإعادة محاولة ورسائل عربية ومنع فتح المحتوى عند الفشل.
- [x] إزالة إذن Android `USE_FULL_SCREEN_INTENT` غير الضروري من إعداد الإصدار 1.0.79.

## تحديث الهوية البصرية — الشعار المرفق

- [x] تهيئة صورة القدر الأخضر المرفقة لتكون أيقونة التطبيق الرسمية بالمقاسات والصيغ المناسبة.
- [x] تحديث `icon.png` و`splash-icon.png` و`favicon.png` و`android-icon-foreground.png` بالشعار الجديد.
- [x] تحديث بيانات الشعار في `app.config.ts` مع الحفاظ على اسم التطبيق ومعرّفه الثابت.
- [x] التحقق من سلامة إعداد Expo وحجم الأصول وتشغيل الفحوص اللازمة.
- [x] حفظ إصدار محدث وإرسال بطاقة تطبيق ألف عافيات للمستخدم.
