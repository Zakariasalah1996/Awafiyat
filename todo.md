# Project TODO - عافيات

- [x] تخصيص لوحة الألوان (theme.config.js) بألوان التطبيق المعتمدة
- [x] تحديث app.config.ts باسم التطبيق والإعدادات
- [x] تصميم شعار التطبيق (Logo) وتحديث أيقونات التطبيق
- [x] بناء شاشة الترحيب (Onboarding) مع الأسئلة الصحية
- [x] بناء نظام التسجيل (الاسم، الهاتف، العمر)
- [x] تصميم الشاشة الرئيسية (5 أقسام رئيسية)
- [x] إعداد الشريط السفلي (3 تبويبات: حسابي، الرئيسية، اشتراكي)
- [x] برمجة قسم "شنو في ثلاجتي" مع الإكمال التلقائي للمكونات العراقية
- [x] ربط قسم الثلاجة بمحرك الذكاء الاصطناعي (Server LLM)
- [x] بناء قاعدة بيانات الوصفات العراقية والعربية (200+ وصفة)
- [x] برمجة مكتبة الوصفات الساحرة (سريعة / دسمة / صحية)
- [x] برمجة شاشة تفاصيل الوصفة (المكونات، الطريقة، السعرات)
- [x] برمجة قسم جدول الطبخ اليومي مع إعداد مواعيد الوجبات
- [x] برمجة قسم "متى وماذا أتسوق" مع قائمة المنتجات والتذكير
- [x] برمجة ميزان السعرات الحرارية
- [x] بناء قسم "حسابي" (المعلومات، الحالة الصحية، أفراد العائلة، الإعدادات)
- [x] بناء قسم "الوصفات المجربة" مع نظام التقييم (5 نجوم)
- [x] بناء قسم "اشتراكي" مع عرض الباقات (شهري/سنوي)
- [x] برمجة نظام الدفع (Zain Cash / Qi Card / بطاقة ائتمان) - واجهة جاهزة، تحتاج ربط API عند الإطلاق
- [x] برمجة نظام الإشعارات (تذكير الوجبات، التسوق، التحفيز)
- [x] دعم المظهر الداكن (Dark Mode)
- [x] دعم RTL الكامل للغة العربية
- [x] صفحة معلومات التطبيق (عن التطبيق، سياسة الخصوصية، اتصل بنا)
- [x] نظام حفظ الوصفات المفضلة
- [x] نظام الصلاحيات (مجاني vs مشترك)

## التعديلات المطلوبة من المستخدم:

- [x] تحديث نظام الوقت إلى 12 ساعة (AM/PM) في جدول الطبخ
- [x] تحسين عرض الوجبات المحفوظة تحت كل وقت في الجدول
- [x] تكبير حجم الخط في جميع الشاشات (خاصة النسخة المجانية)
- [x] تحسين إدارة أفراد العائلة - إضافة/تعديل/حذف
- [x] إضافة قسم جديد للمشروبات (عادية وصحية)
- [x] إضافة 200+ وصفة عراقية وعربية وكردية
- [x] تطبيق نظام الفلترة الذكي - عدم عرض وصفات مضرة للمرض المختار
- [x] إضافة تذكيرات قائمة التسوق (يوم/يومين قبل، مع تحديد الوقت)

## إصلاح ربط الميزات الجديدة بالشاشات:

- [x] ربط قاعدة الوصفات الجديدة (200+) بشاشة مكتبة الوصفات
- [x] ربط قسم المشروبات بالشاشة الرئيسية
- [x] ربط نظام الفلترة الذكي بعرض الوصفات
- [x] ربط إدارة أفراد العائلة بشاشة حسابي
- [x] ربط نظام التذكيرات بشاشة التسوق
- [x] تحديث نظام الوقت إلى 12 ساعة في جدول الطبخ

## تحديثات جديدة - الدفعة الثالثة:

- [x] إنشاء 22 صورة تصنيفات طعام جميلة (شوربات، أرز، لحوم، سلطات، حلويات، مشروبات، إلخ)
- [x] إضافة 250+ وصفة جديدة (كردية، عراقية، خليجية) - صحية وعادية - فطور وغداء وعشاء (333 وصفة إجمالي)
- [x] ربط الصور بالوصفات وتحديث شاشة مكتبة الوصفات وتفاصيل الوصفة لعرض الصور بدل الإيموجي
- [x] إصلاح جدول الطبخ - استبدال حقول الوقت بقائمة منسدلة
- [x] استبدال AM/PM بـ "صباحاً / ظهراً / مساءً" بالعربي
- [x] استبدال كتابة الوقت يدوياً بقائمة منسدلة (picker) لاختيار الوقت

## التوسع الإقليمي - الدفعة الرابعة:

- [x] إضافة شاشة اختيار الدولة بعد الترحيب (العراق، السعودية، الإمارات، مصر)
- [x] حفظ الدولة المختارة في UserProfile
- [x] تعديل البنية اللغوية للتطبيق (الأزرار والأيقونات والترحيب) إلى العربية الفصحى
- [x] إضافة حقل country/origin لكل وصفة بدلاً من isIraqi
- [x] تعديل أسماء جميع الوصفات الحالية لتشمل اسم الدولة (كبة عراقية، تشريب عراقي، إلخ)
- [x] إنشاء 100 وصفة خليجية جديدة (سعودية، إماراتية، كويتية) بلهجة خليجية فصحى
- [x] إنشاء 50 وصفة مشروبات جديدة (بارد/حار، صحي/عادي) بالفصحى
- [x] توليد 15 صورة للوصفات الخليجية الجديدة
- [x] تطبيق نظام الفرز حسب الدولة المختارة (وصفات الدولة تظهر أولاً)
- [x] تفعيل الإشعارات المحلية المجدولة (تعمل حتى لو التطبيق مغلق)
- [x] تعديل رسائل الإشعارات إلى الفصحى

## لوحة التحكم الاحترافية - الدفعة الخامسة:

- [x] إنشاء جداول قاعدة البيانات (اشتراكات، ملاحظات، إشعارات، push tokens)
- [x] بناء API endpoints (إدارة مستخدمين، اشتراكات، وصفات، إشعارات، إحصائيات)
- [x] بناء واجهة لوحة التحكم الويب (Dashboard احترافي)
- [x] إدارة المستخدمين (عرض، تفعيل/تعطيل، تغيير الدور)
- [x] إدارة الاشتراكات (إصدار كوبونات مجانية، عرض المشتركين)
- [x] إدارة الوصفات (إضافة/تعديل/حذف) - عرض معلومات الوصفات
- [x] إرسال إشعارات يدوية للمستخدمين (عبر Expo Push API)
- [x] لوحة الإحصائيات (رسوم بيانية - نمو المستخدمين + توزيع الدول)
- [x] جمع ملاحظات المستخدمين (عرض + تحديث الحالة + ملاحظات المسؤول)
- [x] ربط التطبيق بالخادم (push tokens + ملاحظات)

## تطوير لوحة التحكم - الدفعة السادسة:

- [x] إضافة/تعديل/حذف الوصفات من لوحة التحكم (بحث + فلترة + CRUD كامل)
- [x] رفع صور الوصفات (الصور مربوطة بالتصنيفات)
- [x] إدارة التصنيفات (فلترة حسب التصنيف والأصل)
- [x] تقارير متقدمة (4 رسوم بيانية: تصنيف/أصل/وجبة/صعوبة)
- [x] تصدير البيانات (CSV) للمستخدمين والاشتراكات والوصفات
- [x] إدارة المحتوى (عن التطبيق، الخصوصية، الشروط، بريد التواصل)

## إصلاح بناء APK:

- [x] إضافة extra.eas.projectId في app.config.ts
- [x] إضافة cli.appVersionSource في eas.json

## إصلاح إشعارات Push:

- [x] إصلاح عدم وصول إشعارات Push من لوحة التحكم إلى التطبيق (تم تغيير localhost إلى getApiBaseUrl)
- [x] إضافة استدعاء تلقائي لـ requestNotificationPermissions في _layout.tsx عند بدء التطبيق (كان يُستدعى فقط يدوياً في profile.tsx)
- [x] إضافة setupNotificationListeners في _layout.tsx لاستقبال الإشعارات في الواجهة

## إصلاحات الدفعة السابعة - من ملف PDF + طلب المستخدم:

- [x] إضافة منبه (alarm) بصوت قوي مستمر لأوقات الطبخ (وليس إشعار بسيط)
- [x] تغيير "متى تاكلين الفطور" إلى "متى تعدين الفطور" (وكذلك الغداء والعشاء)
- [x] حفظ أوقات الوجبات في AsyncStorage + إظهار شاشة الأوقات مرة واحدة فقط
- [x] تغيير كلمة "عبي" إلى "تعبئة" أو كلمة فصحى
- [x] إصلاح تكرار الأكلات في الجدول التلقائي (300 وصفة يجب ألا تتكرر)
- [x] حفظ قائمة التسوق بشكل دائم في AsyncStorage
- [x] إزالة "مناسبة لصحتك" وإضافة تحذيرات صحية حسب المرض (ضغط/سكر/كوليسترول/كلى)
- [x] إضافة وصفات سعودية وخليجية (إصلاح بنية الملف + تحويل uae إلى emirati)
- [x] التأكد من عدد الوصفات الفعلية وعدم التكرار (إزالة 111 وصفة مكررة، 255 وصفة فريدة)
- [x] فرز الوصفات حسب دولة المستخدم أولاً
- [x] التحقق من صحة حاسبة السعرات (قيم USDA صحيحة + Harris-Benedict صحيحة)
- [x] إضافة نصائح أكثر في قسم الرعاية الصحية (6 نصائح لكل حالة + 4 نصائح عامة)
- [x] إضافة 25 مشروب إضافي (75 مشروب إجمالي)
- [x] إضافة حفظ العمر والرقم والمعلومات في شاشة حسابي (رسالة تأكيد "تم الحفظ")

## إصلاحات الدفعة الثامنة - جدول الطبخ والمنبه:

- [x] منبه تلقائي يشتغل في الأوقات المحددة (وليس فقط يدوي بالضغط على الزر)
- [x] إضافة صوت منبه حقيقي (وليس فقط اهتزاز)
- [x] إظهار اسم الوصفة المخططة في إشعار المنبه
- [x] فتح صفحة تفاصيل الوصفة عند الضغط على الإشعار

## إصلاحات الدفعة التاسعة - منبه حقيقي بصوت عالي:

- [x] تحويل إشعار الوجبات إلى منبه حقيقي بصوت عالي ومستمر (مثل منبه الساعة) يعمل خارج التطبيق (alarm.wav 30ث + MAX priority + sticky + bypassDnd + full-screen intent permissions)

## إصلاحات الدفعة العاشرة - منبه حقيقي يرن بصوت مستمر:

- [x] عند وصول إشعار الوجبة: تشغيل صوت جرس مستمر عبر expo-audio (loop) + AlarmContext عالمي
- [x] شاشة منبه كاملة (AlarmScreen) تظهر فوق كل شيء مع أنيميشن نبض + زر إيقاف كبير + زر عرض الوصفة
- [x] الصوت يعمل حتى في الوضع الصامت (playsInSilentMode) + اهتزاز متكرر مستمر

## الدفعة 11 - منبه ساعة حقيقي (Native Alarm):

- [x] expo-alarm-module (AlarmManager + MediaPlayer + Foreground Service) لمنبه يرن على مستوى النظام
- [x] ربط المنبه الأصلي بجدولة الوجبات (scheduleMealReminder + cancelMealReminder + AlarmContext)

## الدفعة 12 - إصلاح الإشعارات بالكامل:

- [x] تقوية صوت الإشعار (alarm.wav 15 ثانية بأقصى amplitude + harmonics)
- [x] جعل userId اختياري في جدول push_tokens (nullable) + migration مطبق
- [x] إصلاح تسجيل Push Token بدون تسجيل دخول OAuth + إرسال platform
- [x] إشعارات لوحة التحكم بصوت alarm.wav + priority high + channelId meals

## الدفعة 13 - التحكم في صوت المنبه:

- [x] إنشاء 5 أصوات منبه (جرس مطبخ، كلاسيكي، رقمي، هادئ، عاجل)
- [x] أزرار مستوى الصوت (25%/50%/75%/100%) مع شريط مرئي
- [x] قائمة اختيار النغمة مع معاينة فورية عند الضغط
- [x] زر تفعيل/إيقاف صوت المنبه + الاهتزاز
- [x] حفظ الإعدادات في AsyncStorage
- [x] ربط الإعدادات بـ AlarmContext (مستوى الصوت + النغمة المختارة)

## الدفعة 14 - تعديل التحذيرات الصحية:

- [x] نقل التحذير الصحي للأعلى تحت اسم الوصفة مباشرة (قبل المكونات وطريقة التحضير)
- [x] تغيير صيغة التحذير: "تحذير صحي لك (اسم المرض)" + ذكر المكون الضار بالتحديد
- [x] إزالة "استشر طبيبك" واستبدالها بنصيحة لطيفة ومباشرة
- [x] تطبيق التغيير على جميع الأمراض (ضغط، سكر، كوليسترول، سمنة)

## الدفعة 15 - إصلاح حفظ معلومات المستخدم:

- [x] إضافة زر حفظ واضح وكبير في شاشة حسابي
- [x] التأكد من حفظ جميع المعلومات (الاسم، الهاتف، العمر، الجنس، الدولة، الحالة الصحية) في AsyncStorage
- [x] رسالة تأكيد عند الحفظ بنجاح

## الدفعة 16 - إصلاح إشعارات التسوق:

- [x] حذف إشعار الثلاجة من شاشة حسابي (لا فائدة منه)
- [x] إضافة زر حفظ القائمة في شاشة التسوق
- [x] إضافة تذكير حقيقي بموعد التسوق (يسأل المستخدم متى يريد التذكير + يضبط إشعار فعلي)
- [x] حفظ موعد التذكير في AsyncStorage

## الدفعة 17 - إصلاح إرسال الإشعارات من لوحة التحكم:

- [x] فحص كود السيرفر لإرسال الإشعارات وتحديد المشكلة
- [x] إصلاح تسجيل Push Token في التطبيق (retry 3 مرات + حفظ في AsyncStorage + إعادة تسجيل عند كل فتح)
- [x] إضافة مؤشر حالة الإشعارات في شاشة حسابي (مسجل/غير مسجل + زر تسجيل يدوي)
- [x] إضافة عداد الأجهزة المسجلة في لوحة التحكم (صفحة الإشعارات)

## الدفعة 18 - رفع صور الوصفات من لوحة التحكم
- [x] إضافة endpoint رفع صور في السيرفر (S3 upload)
- [x] إضافة حقل رفع صورة + معاينة في نموذج إضافة/تعديل الوصفة بلوحة التحكم
- [x] تعديل recipes-api لحفظ رابط الصورة المرفوعة مع الوصفة
- [x] التطبيق يعرض الصورة المرفوعة بدل الصورة الافتراضية

## الدفعة 19 - صورة مصغرة في جدول الوصفات
- [x] إضافة عمود "الصورة" في جدول الوصفات بلوحة التحكم لعرض صورة مصغرة

## الدفعة 20 - إصلاح حذف البيانات عند الانتقال بين الحقول
- [x] إصلاح مشكلة: البيانات تُحذف عند الانتقال من حقل لآخر في شاشة حسابي

## الدفعة 21 - إصلاح صور الوصفات + تسجيل تلقائي كضيف
- [x] إصلاح أخطاء recipes.ts
- [x] ربط صور الوصفات المرفوعة من لوحة التحكم بالتطبيق (جلب الصور من السيرفر)
- [x] تسجيل تلقائي كضيف في قاعدة البيانات بدون OAuth

## الدفعة 22 - إصلاح Push Token لـ APK غير EAS:

- [x] إضافة getDevicePushTokenAsync كـ fallback للحصول على FCM Token مباشرة (يعمل بدون EAS Build)
- [x] السيرفر يدعم الآن كلا النوعين: ExponentPushToken و fcm: tokens
- [x] إصلاح cleanup endpoint لعدم حذف FCM tokens

## الدفعة 23 - إصلاح عدم ظهور الصور المرفوعة للوصفات:

- [x] تشخيص المشكلة: الـ endpoint `/api/recipes/images` كان يرفض الصور التي لا تبدأ بـ `http://` أو `https://`
- [x] إصلاح الـ endpoint ليقبل جميع الصور (بما فيها URLs من التخزين)
- [x] التحقق من عدم وجود أخطاء TypeScript

## الدفعة 24 - صوت منبه الفطور الصباحي:

- [x] نسخ الملف الصوتي (صوتمنبهصباحي.mp3) إلى assets/alarm_morning.mp3
- [x] إضافة نغمة "منبه الفطور الصباحي" في AlarmContext كخيار أول في القائمة
- [x] جعل منبه الفطور يشغّل الصوت الصباحي تلقائياً بغض النظر عن النغمة المختارة
- [x] تحديث إشعار الفطور في notifications.ts ليستخدم alarm_morning.mp3
- [x] إضافة alarm_morning.mp3 في قائمة أصوات expo-notifications في app.config.ts

## الدفعة 28 - إصلاح اتصال APK بالسيرفر المنشور:

- [ ] إصلاح عدم اتصال APK بالسيرفر المنشور (لا إشعارات، لا صور وصفات، لا تسجيل مستخدم)

## الدفعة 29 - إصلاح نظام الإشعارات نهائياً (FCM V1 API مباشر):

- [x] تعديل lib/notifications.ts: استخدام getDevicePushTokenAsync() أولاً (FCM token مباشر) بدلاً من getExpoPushTokenAsync()
- [x] تعديل server/_core/index.ts: إضافة دالة sendPushViaFCM() للإرسال عبر Firebase FCM V1 API مباشرة
- [x] تحديث endpoint /api/admin/notifications/send لاستخدام FCM V1 API بدلاً من Expo Push API
- [x] تنظيف التوكنات القديمة (ExponentPushToken + test tokens) من قاعدة البيانات
- [ ] حفظ checkpoint وبناء APK جديد عبر EAS Build
