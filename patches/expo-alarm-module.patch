diff --git a/android/src/main/java/com/expoalarmmodule/Alarm.java b/android/src/main/java/com/expoalarmmodule/Alarm.java
index 49daf38ac5620031fd3d1da8c52c0762d822b96a..b892397ea73283a58a0b2be1ca73e957ee728485 100644
--- a/android/src/main/java/com/expoalarmmodule/Alarm.java
+++ b/android/src/main/java/com/expoalarmmodule/Alarm.java
@@ -55,7 +55,12 @@ public class Alarm implements Cloneable {
     String dismissText;
     String snoozeText;
 
-    Alarm(String uid, ArrayList<Integer> days, ZonedDateTime date, int hour, int minutes, boolean showDismiss, boolean showSnooze, int snoozeInterval, String title, String description, boolean repeating, boolean active, String dismissText, String snoozeText) {
+    /**
+     * Custom sound name (matches res/raw/ file). Default is "default" (system ringtone).
+     */
+    String sound;
+
+    Alarm(String uid, ArrayList<Integer> days, ZonedDateTime date, int hour, int minutes, boolean showDismiss, boolean showSnooze, int snoozeInterval, String title, String description, boolean repeating, boolean active, String dismissText, String snoozeText, String sound) {
         this.uid = uid;
         this.days = days;
         this.hour = hour;
@@ -69,6 +74,7 @@ public class Alarm implements Cloneable {
         this.active = active;
         this.dismissText = dismissText;
         this.snoozeText = snoozeText;
+        this.sound = (sound != null && !sound.isEmpty()) ? sound : "default";
         if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
           this.date = date;
         }
diff --git a/android/src/main/java/com/expoalarmmodule/ExpoAlarmModuleModule.java b/android/src/main/java/com/expoalarmmodule/ExpoAlarmModuleModule.java
index 552911d58dbc4123c3eff79031d88bd8e4dc1ed9..94c0b843f9bae692266a4418cd25eafef943ffda 100644
--- a/android/src/main/java/com/expoalarmmodule/ExpoAlarmModuleModule.java
+++ b/android/src/main/java/com/expoalarmmodule/ExpoAlarmModuleModule.java
@@ -155,6 +155,11 @@ public class ExpoAlarmModuleModule extends ReactContextBaseJavaModule {
     String dismissText = alarm.hasKey("dismissText") ? alarm.getString("dismissText") : "Dismiss";
     String snoozeText = alarm.hasKey("snoozeText") ? alarm.getString("snoozeText") : "Snooze";
 
+    /**
+     * Custom sound name (matches res/raw/ file)
+     */
+    String sound = alarm.hasKey("sound") ? alarm.getString("sound") : "default";
+
     ArrayList<Integer> days = new ArrayList<>();
 
     ZonedDateTime date = null;
@@ -176,7 +181,7 @@ public class ExpoAlarmModuleModule extends ReactContextBaseJavaModule {
         }
       }
     }
-    return new Alarm(uid, days, date, hour, minutes, showDismiss, showSnooze, snoozeInterval, title, description, repeating, active, dismissText, snoozeText);
+    return new Alarm(uid, days, date, hour, minutes, showDismiss, showSnooze, snoozeInterval, title, description, repeating, active, dismissText, snoozeText, sound);
   }
 
   private WritableMap serializeAlarmObject (Alarm alarm) throws Exception {
@@ -196,7 +201,8 @@ public class ExpoAlarmModuleModule extends ReactContextBaseJavaModule {
     map.putString("dismissText", alarm.dismissText);
     map.putString("snoozeText", alarm.snoozeText);
 
-
+    // Custom sound
+    map.putString("sound", alarm.sound != null ? alarm.sound : "default");
 
     if( alarm.date != null) {
       if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
diff --git a/android/src/main/java/com/expoalarmmodule/Manager.java b/android/src/main/java/com/expoalarmmodule/Manager.java
index 004075da7464088c26edd8b5e6e2e5a191ec7633..a94800f0638b61fc1373fa54dd91fecc8ebe65b0 100644
--- a/android/src/main/java/com/expoalarmmodule/Manager.java
+++ b/android/src/main/java/com/expoalarmmodule/Manager.java
@@ -114,9 +114,15 @@ public class Manager {
     static void start(Context context, String alarmUid) {
         activeAlarmUid = alarmUid;
         sound = new Sound(context);
-        sound.play("default");
+        // Read custom sound from alarm object, fallback to "default"
+        Alarm alarm = Storage.getAlarm(context, alarmUid);
+        String soundName = "default";
+        if (alarm != null && alarm.sound != null && !alarm.sound.isEmpty()) {
+            soundName = alarm.sound;
+        }
+        sound.play(soundName);
 
-        Log.d(TAG, "Starting " + activeAlarmUid);
+        Log.d(TAG, "Starting " + activeAlarmUid + " with sound: " + soundName);
     }
 
     public static void stop(Context context) {
