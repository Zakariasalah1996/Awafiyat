diff --git a/android/src/main/java/com/expoalarmmodule/Alarm.java b/android/src/main/java/com/expoalarmmodule/Alarm.java
index 49daf38ac5620031fd3d1da8c52c0762d822b96a..5e378b19779cbb44f3c542251d90e56c655b30d4 100644
--- a/android/src/main/java/com/expoalarmmodule/Alarm.java
+++ b/android/src/main/java/com/expoalarmmodule/Alarm.java
@@ -19,43 +19,48 @@ import static com.expoalarmmodule.GsonUtil.createSerialize;
 
 public class Alarm implements Cloneable {
 
-    String uid;
+    public String uid;
 
-    ZonedDateTime date;
-    String dateString;
+    public ZonedDateTime date;
+    public String dateString;
 
-    ArrayList<Integer> days;
-    int hour;
-    int minutes;
+    public ArrayList<Integer> days;
+    public int hour;
+    public int minutes;
 
-    String title;
-    String description;
-    boolean repeating;
-    boolean active;
+    public String title;
+    public String description;
+    public boolean repeating;
+    public boolean active;
     
     /**
      * Show dismiss button in notification. It is false by default.
      */
-    boolean showDismiss;
+    public boolean showDismiss;
 
     /**
      * Show snooze button. It is false by default.
      */
-    boolean showSnooze;
+    public boolean showSnooze;
 
     /**
      * Snooze interval in minutes
      * Default is 5 minutes
      */
-    int snoozeInterval;
+    public int snoozeInterval;
 
     /**
      * Custom texts for buttons of notifications
      */
-    String dismissText;
-    String snoozeText;
+    public String dismissText;
+    public String snoozeText;
 
-    Alarm(String uid, ArrayList<Integer> days, ZonedDateTime date, int hour, int minutes, boolean showDismiss, boolean showSnooze, int snoozeInterval, String title, String description, boolean repeating, boolean active, String dismissText, String snoozeText) {
+    /**
+     * Custom sound name (matches res/raw/ file). Default is "default" (system ringtone).
+     */
+    public String sound;
+
+    public Alarm(String uid, ArrayList<Integer> days, ZonedDateTime date, int hour, int minutes, boolean showDismiss, boolean showSnooze, int snoozeInterval, String title, String description, boolean repeating, boolean active, String dismissText, String snoozeText, String sound) {
         this.uid = uid;
         this.days = days;
         this.hour = hour;
@@ -69,6 +74,7 @@ public class Alarm implements Cloneable {
         this.active = active;
         this.dismissText = dismissText;
         this.snoozeText = snoozeText;
+        this.sound = (sound != null && !sound.isEmpty()) ? sound : "default";
         if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
           this.date = date;
         }
diff --git a/android/src/main/java/com/expoalarmmodule/ExpoAlarmModuleModule.java b/android/src/main/java/com/expoalarmmodule/ExpoAlarmModuleModule.java
index 552911d58dbc4123c3eff79031d88bd8e4dc1ed9..c8d4ea3ae531e295f377433b0ec6fdff743957f3 100644
--- a/android/src/main/java/com/expoalarmmodule/ExpoAlarmModuleModule.java
+++ b/android/src/main/java/com/expoalarmmodule/ExpoAlarmModuleModule.java
@@ -155,6 +155,11 @@ public class ExpoAlarmModuleModule extends ReactContextBaseJavaModule {
     String dismissText = alarm.hasKey("dismissText") ? alarm.getString("dismissText") : "Dismiss";
     String snoozeText = alarm.hasKey("snoozeText") ? alarm.getString("snoozeText") : "Snooze";
 
+    /**
+     * Custom sound name (matches res/raw/ file)
+     */
+    String sound = alarm.hasKey("sound") ? alarm.getString("sound") : "default";
+
     ArrayList<Integer> days = new ArrayList<>();
 
     ZonedDateTime date = null;
@@ -176,7 +181,7 @@ public class ExpoAlarmModuleModule extends ReactContextBaseJavaModule {
         }
       }
     }
-    return new Alarm(uid, days, date, hour, minutes, showDismiss, showSnooze, snoozeInterval, title, description, repeating, active, dismissText, snoozeText);
+    return new Alarm(uid, days, date, hour, minutes, showDismiss, showSnooze, snoozeInterval, title, description, repeating, active, dismissText, snoozeText, sound);
   }
 
   private WritableMap serializeAlarmObject (Alarm alarm) throws Exception {
@@ -196,7 +201,8 @@ public class ExpoAlarmModuleModule extends ReactContextBaseJavaModule {
     map.putString("dismissText", alarm.dismissText);
     map.putString("snoozeText", alarm.snoozeText);
 
-
+    // Custom sound
+    map.putString("sound", alarm.sound != null ? alarm.sound : "default");
 
     if( alarm.date != null) {
       if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
diff --git a/android/src/main/java/com/expoalarmmodule/Helper.java b/android/src/main/java/com/expoalarmmodule/Helper.java
index 1c5b665000d21998b8ccfb8ff51e9041cc4c640b..2f4992fd1c7ee3268c120ac09dc13a7113eba7da 100644
--- a/android/src/main/java/com/expoalarmmodule/Helper.java
+++ b/android/src/main/java/com/expoalarmmodule/Helper.java
@@ -122,8 +122,7 @@ public class Helper {
             // Sets the notification light color for notifications posted to this
             // channel, if the device supports this feature.
             channel.setLightColor(Color.RED);
-            channel.enableVibration(true);
-            channel.setVibrationPattern(new long[]{100, 200, 300, 400, 500, 400, 300, 200, 400});
+            channel.enableVibration(false);
             // Register the channel with the system; you can't change the importance
             // or other notification behaviors after this
             NotificationManager notificationManager = ContextCompat.getSystemService(context, NotificationManager.class);
@@ -157,7 +156,8 @@ public class Helper {
                 .setContentTitle(title)
                 .setTicker(null)
                 .setContentText(description)
-                .setDefaults(NotificationCompat.DEFAULT_ALL)
+                .setDefaults(NotificationCompat.DEFAULT_LIGHTS)
+                .setFullScreenIntent(createOnClickedIntent(context, alarmUid, id), true)
                 .setPriority(NotificationCompat.PRIORITY_MAX)
                 .setVisibility(NotificationCompat.VISIBILITY_PUBLIC)
                 .setCategory(NotificationCompat.CATEGORY_ALARM)
@@ -173,10 +173,10 @@ public class Helper {
             builder.addAction(android.R.drawable.ic_menu_close_clear_cancel, dismissText, pendingIntentDismiss);
         }
 
-        // If snooze is active, add a button for snoozing.
+        // View recipe button - opens the app and shows the recipe
         if(showSnooze) {
-            PendingIntent pendingIntentSnoozee = createActionIntent(context, alarmUid, id, "SNOOZE_ACTION");
-            builder.addAction(android.R.drawable.ic_menu_recent_history, snoozeText, pendingIntentSnoozee);
+            PendingIntent pendingIntentViewRecipe = createActionIntent(context, alarmUid, id, "VIEW_RECIPE_ACTION");
+            builder.addAction(android.R.drawable.ic_menu_view, snoozeText, pendingIntentViewRecipe);
         }
 
     
diff --git a/android/src/main/java/com/expoalarmmodule/Manager.java b/android/src/main/java/com/expoalarmmodule/Manager.java
index 004075da7464088c26edd8b5e6e2e5a191ec7633..ac539f24be71a3c4aec52bfbf6b941b66556bddd 100644
--- a/android/src/main/java/com/expoalarmmodule/Manager.java
+++ b/android/src/main/java/com/expoalarmmodule/Manager.java
@@ -114,9 +114,15 @@ public class Manager {
     static void start(Context context, String alarmUid) {
         activeAlarmUid = alarmUid;
         sound = new Sound(context);
-        sound.play("default");
+        // Read custom sound from alarm object, fallback to "default"
+        Alarm alarm = Storage.getAlarm(context, alarmUid);
+        String soundName = "default";
+        if (alarm != null && alarm.sound != null && !alarm.sound.isEmpty()) {
+            soundName = alarm.sound;
+        }
+        sound.play(soundName);
 
-        Log.d(TAG, "Starting " + activeAlarmUid);
+        Log.d(TAG, "Starting " + activeAlarmUid + " with sound: " + soundName);
     }
 
     public static void stop(Context context) {
diff --git a/android/src/main/java/com/expoalarmmodule/Sound.java b/android/src/main/java/com/expoalarmmodule/Sound.java
index 73b00616707cd5533258ce215fb1fb1e2deafb50..b84a5af5851bd1cd54678ede5ef6af228d6d429f 100644
--- a/android/src/main/java/com/expoalarmmodule/Sound.java
+++ b/android/src/main/java/com/expoalarmmodule/Sound.java
@@ -4,27 +4,21 @@ import android.content.Context;
 import android.media.AudioManager;
 import android.media.MediaPlayer;
 import android.net.Uri;
-import android.os.Build;
-import android.os.VibrationEffect;
-import android.os.Vibrator;
 import android.provider.Settings;
 import android.util.Log;
 
 class Sound {
 
     private static final String TAG = "AlarmSound";
-    private static final long DEFAULT_VIBRATION = 100;
 
     private AudioManager audioManager;
     private int userVolume;
     private MediaPlayer mediaPlayer;
-    private Vibrator vibrator;
 
     private Context context;
 
     Sound(Context context) {
         this.context = context;
-        this.vibrator = (Vibrator) context.getSystemService(Context.VIBRATOR_SERVICE);
         this.audioManager = (AudioManager) context.getSystemService(Context.AUDIO_SERVICE);
         this.userVolume = audioManager.getStreamVolume(AudioManager.STREAM_ALARM);
         this.mediaPlayer = new MediaPlayer();
@@ -33,14 +27,12 @@ class Sound {
     void play(String sound) {
         Uri soundUri = getSoundUri(sound);
         playSound(soundUri);
-        startVibration();
     }
 
     void stop() {
         try {
             if (mediaPlayer.isPlaying()) {
                 stopSound();
-                stopVibration();
                 mediaPlayer.release();
             }
         } catch (IllegalStateException e) {
@@ -76,26 +68,6 @@ class Sound {
         }
     }
 
-    private void startVibration() {
-        vibrator.vibrate(DEFAULT_VIBRATION);
-        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
-            vibrator.vibrate(VibrationEffect.createOneShot(5000, VibrationEffect.DEFAULT_AMPLITUDE));
-        } else {
-            //deprecated in API 26
-            vibrator.vibrate(500);
-        }
-
-        long[] pattern = {0, 100, 1000};
-
-        // The '0' here means to repeat indefinitely
-        // '0' is actually the index at which the pattern keeps repeating from (the start)
-        // To repeat the pattern from any other point, you could increase the index, e.g. '1'
-        vibrator.vibrate(pattern, 0);
-    }
-
-    private void stopVibration() {
-        vibrator.cancel();
-    }
 
     private Uri getSoundUri(String soundName) {
         Uri soundUri;
diff --git a/android/src/main/java/com/expoalarmmodule/receivers/NotificationActionReceiver.java b/android/src/main/java/com/expoalarmmodule/receivers/NotificationActionReceiver.java
index 0d2dba4f94c5f065f1f58accb4fa3415643b2bf6..1fd1e77563027fe947c68a2506c31bf60ad656d6 100644
--- a/android/src/main/java/com/expoalarmmodule/receivers/NotificationActionReceiver.java
+++ b/android/src/main/java/com/expoalarmmodule/receivers/NotificationActionReceiver.java
@@ -44,6 +44,38 @@ public class NotificationActionReceiver extends BroadcastReceiver {
                 this.removeNotification(context, notificationId);
                 break;
 
+            case "VIEW_RECIPE_ACTION":
+                Log.d(TAG, "Received VIEW_RECIPE action for Alarm: " + alarmUid);
+                Manager.stop(context);
+                this.removeNotification(context, notificationId);
+                // قراءة بيانات المنبه من Storage لاستخراج recipeId
+                com.expoalarmmodule.Alarm viewAlarm = com.expoalarmmodule.Storage.getAlarm(context, alarmUid);
+                String recipeId = null;
+                if (viewAlarm != null && viewAlarm.description != null) {
+                    String desc = viewAlarm.description;
+                    if (desc.startsWith("RECIPE_ID:")) {
+                        int pipeIdx = desc.indexOf("|");
+                        recipeId = (pipeIdx > 0) ? desc.substring(10, pipeIdx) : desc.substring(10);
+                        Log.d(TAG, "Extracted recipeId: " + recipeId);
+                    }
+                }
+                if (recipeId != null && !recipeId.isEmpty()) {
+                    // فتح التطبيق مباشرةً على صفحة الوصفة عبر deep link
+                    String deepLink = "manus20260413170437://sections/recipe-detail?id=" + recipeId;
+                    Log.d(TAG, "Opening deep link: " + deepLink);
+                    Intent deepLinkIntent = new Intent(Intent.ACTION_VIEW, android.net.Uri.parse(deepLink));
+                    deepLinkIntent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK | Intent.FLAG_ACTIVITY_CLEAR_TOP);
+                    context.startActivity(deepLinkIntent);
+                } else {
+                    // لا يوجد recipeId - افتح التطبيق فقط
+                    Intent launchIntent = context.getPackageManager().getLaunchIntentForPackage(context.getPackageName());
+                    if (launchIntent != null) {
+                        launchIntent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK | Intent.FLAG_ACTIVITY_CLEAR_TOP);
+                        context.startActivity(launchIntent);
+                    }
+                }
+                break;
+
             default:
                 Log.e(TAG, "Unknown action received: " + action);
                 break;
diff --git a/lib/commonjs/models/Alarm.js b/lib/commonjs/models/Alarm.js
index 125b8e88fe8e1b460734d1add901e0aa3aa80881..b71a0992cc59a0d0db076e409f9d412c501663ba 100644
--- a/lib/commonjs/models/Alarm.js
+++ b/lib/commonjs/models/Alarm.js
@@ -20,6 +20,7 @@ class Alarm {
     this.repeating = (0, _utils.getParam)(params, 'repeating');
     this.active = (0, _utils.getParam)(params, 'active');
     this.day = (0, _utils.getParam)(params, 'day');
+    this.sound = (0, _utils.getParam)(params, 'sound');
   }
   static getEmpty() {
     return new Alarm({
diff --git a/lib/module/models/Alarm.js b/lib/module/models/Alarm.js
index 5378c0ab6d1e1ff1335e3740d13243549a07b77a..37a8d7116bd702979f99cff554219a64509375cd 100644
--- a/lib/module/models/Alarm.js
+++ b/lib/module/models/Alarm.js
@@ -14,6 +14,7 @@ class Alarm {
     this.repeating = getParam(params, 'repeating');
     this.active = getParam(params, 'active');
     this.day = getParam(params, 'day');
+    this.sound = getParam(params, 'sound');
   }
   static getEmpty() {
     return new Alarm({
diff --git a/src/models/Alarm.tsx b/src/models/Alarm.tsx
index c0bd28157e94e70e7c701f19718d90801069f913..d265d1360701ecabba21e28caa978d2f0af1001a 100644
--- a/src/models/Alarm.tsx
+++ b/src/models/Alarm.tsx
@@ -14,6 +14,7 @@ class Alarm {
   repeating?: boolean | undefined;
   active?: boolean | undefined;
   day?: string | Date | number[] | undefined;
+  sound?: string | undefined;
 
   constructor(params: any = null) {
     this.uid = getParam(params, 'uid');
@@ -29,6 +30,7 @@ class Alarm {
     this.repeating = getParam(params, 'repeating');
     this.active = getParam(params, 'active');
     this.day = getParam(params, 'day');
+    this.sound = getParam(params, 'sound');
   }
 
   static getEmpty() {
