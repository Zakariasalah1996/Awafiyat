diff --git a/android/lib/commonjs/main/java/com/expoalarmmodule/Alarm.java b/android/lib/commonjs/main/java/com/expoalarmmodule/Alarm.java
index 49daf38ac5620031fd3d1da8c52c0762d822b96a..30c0a194750c29691b2bebcb9f9a79da5dfd2a34 100644
--- a/android/lib/commonjs/main/java/com/expoalarmmodule/Alarm.java
+++ b/android/lib/commonjs/main/java/com/expoalarmmodule/Alarm.java
@@ -54,8 +54,9 @@ public class Alarm implements Cloneable {
      */
     String dismissText;
     String snoozeText;
+    String sound;
 
-    Alarm(String uid, ArrayList<Integer> days, ZonedDateTime date, int hour, int minutes, boolean showDismiss, boolean showSnooze, int snoozeInterval, String title, String description, boolean repeating, boolean active, String dismissText, String snoozeText) {
+    Alarm(String uid, ArrayList<Integer> days, ZonedDateTime date, int hour, int minutes, boolean showDismiss, boolean showSnooze, int snoozeInterval, String title, String description, boolean repeating, boolean active, String dismissText, String snoozeText, String sound) {
         this.uid = uid;
         this.days = days;
         this.hour = hour;
@@ -69,6 +70,7 @@ public class Alarm implements Cloneable {
         this.active = active;
         this.dismissText = dismissText;
         this.snoozeText = snoozeText;
+        this.sound = sound;
         if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
           this.date = date;
         }
@@ -99,12 +101,20 @@ public class Alarm implements Cloneable {
     @RequiresApi(api = Build.VERSION_CODES.O)
     static Alarm fromJson(String json) {
       Alarm alarmTemp = createSerialize().fromJson(json, Alarm.class);
-      alarmTemp.date = ZonedDateTime.parse(alarmTemp.dateString);
+      if (alarmTemp.dateString != null && !alarmTemp.dateString.isEmpty()) {
+          try {
+              alarmTemp.date = ZonedDateTime.parse(alarmTemp.dateString);
+          } catch (Exception e) {
+              alarmTemp.date = null;
+          }
+      } else {
+          alarmTemp.date = null;
+      }
       return alarmTemp;
     }
 
     static String toJson(Alarm alarm) {
-        alarm.dateString = alarm.date.toString();
+        alarm.dateString = alarm.date != null ? alarm.date.toString() : null;
         return createSerialize().toJson(alarm);
     }
 
diff --git a/android/lib/commonjs/main/java/com/expoalarmmodule/ExpoAlarmModuleModule.java b/android/lib/commonjs/main/java/com/expoalarmmodule/ExpoAlarmModuleModule.java
index 552911d58dbc4123c3eff79031d88bd8e4dc1ed9..c603bd74bc8f9efe240eec7a4b5ddc0477d320eb 100644
--- a/android/lib/commonjs/main/java/com/expoalarmmodule/ExpoAlarmModuleModule.java
+++ b/android/lib/commonjs/main/java/com/expoalarmmodule/ExpoAlarmModuleModule.java
@@ -154,6 +154,7 @@ public class ExpoAlarmModuleModule extends ReactContextBaseJavaModule {
      */
     String dismissText = alarm.hasKey("dismissText") ? alarm.getString("dismissText") : "Dismiss";
     String snoozeText = alarm.hasKey("snoozeText") ? alarm.getString("snoozeText") : "Snooze";
+    String sound = alarm.hasKey("sound") ? alarm.getString("sound") : "default";
 
     ArrayList<Integer> days = new ArrayList<>();
 
@@ -176,7 +177,7 @@ public class ExpoAlarmModuleModule extends ReactContextBaseJavaModule {
         }
       }
     }
-    return new Alarm(uid, days, date, hour, minutes, showDismiss, showSnooze, snoozeInterval, title, description, repeating, active, dismissText, snoozeText);
+    return new Alarm(uid, days, date, hour, minutes, showDismiss, showSnooze, snoozeInterval, title, description, repeating, active, dismissText, snoozeText, sound);
   }
 
   private WritableMap serializeAlarmObject (Alarm alarm) throws Exception {
diff --git a/android/lib/commonjs/main/java/com/expoalarmmodule/Helper.java b/android/lib/commonjs/main/java/com/expoalarmmodule/Helper.java
index 1c5b665000d21998b8ccfb8ff51e9041cc4c640b..004b6cac4c169fbdcf8baae190acf317f383d7f2 100644
--- a/android/lib/commonjs/main/java/com/expoalarmmodule/Helper.java
+++ b/android/lib/commonjs/main/java/com/expoalarmmodule/Helper.java
@@ -51,12 +51,23 @@ public class Helper {
             );
         }
 
-        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.M) {
-            alarmManager.setExactAndAllowWhileIdle(AlarmManager.RTC_WAKEUP, triggerAtMillis, pendingIntent);
-        } else if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.KITKAT) {
-            alarmManager.setExact(AlarmManager.RTC_WAKEUP, triggerAtMillis, pendingIntent);
+        // setAlarmClock: guaranteed delivery on ALL Android versions (no permissions needed)
+        // Does NOT get affected by Doze, Battery Optimization, or Samsung restrictions
+        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.LOLLIPOP) {
+            Intent showIntent = new Intent(context, Helper.getMainActivityClass(context));
+            showIntent.putExtra("ALARM_UID", alarmUid);
+            PendingIntent showPendingIntent;
+            if (android.os.Build.VERSION.SDK_INT >= android.os.Build.VERSION_CODES.S) {
+                showPendingIntent = PendingIntent.getActivity(context, notificationID + 10000, showIntent, PendingIntent.FLAG_MUTABLE);
+            } else {
+                showPendingIntent = PendingIntent.getActivity(context, notificationID + 10000, showIntent, PendingIntent.FLAG_UPDATE_CURRENT);
+            }
+            AlarmManager.AlarmClockInfo alarmClockInfo = new AlarmManager.AlarmClockInfo(triggerAtMillis, showPendingIntent);
+            alarmManager.setAlarmClock(alarmClockInfo, pendingIntent);
+            Log.d(TAG, "Using setAlarmClock (guaranteed delivery, no permissions needed)");
         } else {
-            alarmManager.set(AlarmManager.RTC_WAKEUP, triggerAtMillis, pendingIntent);
+            alarmManager.setExact(AlarmManager.RTC_WAKEUP, triggerAtMillis, pendingIntent);
+            Log.d(TAG, "Using setExact (pre-Lollipop fallback)");
         }
         Log.d(TAG, "SDK version: " + Build.VERSION.SDK_INT);
         Log.d(TAG, "scheduling alarm with notification id: " + notificationID);
diff --git a/android/lib/commonjs/main/java/com/expoalarmmodule/Manager.java b/android/lib/commonjs/main/java/com/expoalarmmodule/Manager.java
index 004075da7464088c26edd8b5e6e2e5a191ec7633..c65a8c756aa605e571daba3e4386a4c33d15a9b2 100644
--- a/android/lib/commonjs/main/java/com/expoalarmmodule/Manager.java
+++ b/android/lib/commonjs/main/java/com/expoalarmmodule/Manager.java
@@ -114,9 +114,11 @@ public class Manager {
     static void start(Context context, String alarmUid) {
         activeAlarmUid = alarmUid;
         sound = new Sound(context);
-        sound.play("default");
-
-        Log.d(TAG, "Starting " + activeAlarmUid);
+        // Use the alarm's custom sound if available
+        Alarm alarm = Storage.getAlarm(context, alarmUid);
+        String soundName = (alarm != null && alarm.sound != null && !alarm.sound.isEmpty()) ? alarm.sound : "default";
+        sound.play(soundName);
+        Log.d(TAG, "Starting " + activeAlarmUid + " with sound: " + soundName);
     }
 
     public static void stop(Context context) {
diff --git a/android/src/main/java/com/expoalarmmodule/Alarm.java b/android/src/main/java/com/expoalarmmodule/Alarm.java
index 49daf38ac5620031fd3d1da8c52c0762d822b96a..30c0a194750c29691b2bebcb9f9a79da5dfd2a34 100644
--- a/android/src/main/java/com/expoalarmmodule/Alarm.java
+++ b/android/src/main/java/com/expoalarmmodule/Alarm.java
@@ -54,8 +54,9 @@ public class Alarm implements Cloneable {
      */
     String dismissText;
     String snoozeText;
+    String sound;
 
-    Alarm(String uid, ArrayList<Integer> days, ZonedDateTime date, int hour, int minutes, boolean showDismiss, boolean showSnooze, int snoozeInterval, String title, String description, boolean repeating, boolean active, String dismissText, String snoozeText) {
+    Alarm(String uid, ArrayList<Integer> days, ZonedDateTime date, int hour, int minutes, boolean showDismiss, boolean showSnooze, int snoozeInterval, String title, String description, boolean repeating, boolean active, String dismissText, String snoozeText, String sound) {
         this.uid = uid;
         this.days = days;
         this.hour = hour;
@@ -69,6 +70,7 @@ public class Alarm implements Cloneable {
         this.active = active;
         this.dismissText = dismissText;
         this.snoozeText = snoozeText;
+        this.sound = sound;
         if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
           this.date = date;
         }
@@ -99,12 +101,20 @@ public class Alarm implements Cloneable {
     @RequiresApi(api = Build.VERSION_CODES.O)
     static Alarm fromJson(String json) {
       Alarm alarmTemp = createSerialize().fromJson(json, Alarm.class);
-      alarmTemp.date = ZonedDateTime.parse(alarmTemp.dateString);
+      if (alarmTemp.dateString != null && !alarmTemp.dateString.isEmpty()) {
+          try {
+              alarmTemp.date = ZonedDateTime.parse(alarmTemp.dateString);
+          } catch (Exception e) {
+              alarmTemp.date = null;
+          }
+      } else {
+          alarmTemp.date = null;
+      }
       return alarmTemp;
     }
 
     static String toJson(Alarm alarm) {
-        alarm.dateString = alarm.date.toString();
+        alarm.dateString = alarm.date != null ? alarm.date.toString() : null;
         return createSerialize().toJson(alarm);
     }
 
diff --git a/android/src/main/java/com/expoalarmmodule/ExpoAlarmModuleModule.java b/android/src/main/java/com/expoalarmmodule/ExpoAlarmModuleModule.java
index 552911d58dbc4123c3eff79031d88bd8e4dc1ed9..c603bd74bc8f9efe240eec7a4b5ddc0477d320eb 100644
--- a/android/src/main/java/com/expoalarmmodule/ExpoAlarmModuleModule.java
+++ b/android/src/main/java/com/expoalarmmodule/ExpoAlarmModuleModule.java
@@ -154,6 +154,7 @@ public class ExpoAlarmModuleModule extends ReactContextBaseJavaModule {
      */
     String dismissText = alarm.hasKey("dismissText") ? alarm.getString("dismissText") : "Dismiss";
     String snoozeText = alarm.hasKey("snoozeText") ? alarm.getString("snoozeText") : "Snooze";
+    String sound = alarm.hasKey("sound") ? alarm.getString("sound") : "default";
 
     ArrayList<Integer> days = new ArrayList<>();
 
@@ -176,7 +177,7 @@ public class ExpoAlarmModuleModule extends ReactContextBaseJavaModule {
         }
       }
     }
-    return new Alarm(uid, days, date, hour, minutes, showDismiss, showSnooze, snoozeInterval, title, description, repeating, active, dismissText, snoozeText);
+    return new Alarm(uid, days, date, hour, minutes, showDismiss, showSnooze, snoozeInterval, title, description, repeating, active, dismissText, snoozeText, sound);
   }
 
   private WritableMap serializeAlarmObject (Alarm alarm) throws Exception {
diff --git a/android/src/main/java/com/expoalarmmodule/Helper.java b/android/src/main/java/com/expoalarmmodule/Helper.java
index 1c5b665000d21998b8ccfb8ff51e9041cc4c640b..004b6cac4c169fbdcf8baae190acf317f383d7f2 100644
--- a/android/src/main/java/com/expoalarmmodule/Helper.java
+++ b/android/src/main/java/com/expoalarmmodule/Helper.java
@@ -51,12 +51,23 @@ public class Helper {
             );
         }
 
-        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.M) {
-            alarmManager.setExactAndAllowWhileIdle(AlarmManager.RTC_WAKEUP, triggerAtMillis, pendingIntent);
-        } else if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.KITKAT) {
-            alarmManager.setExact(AlarmManager.RTC_WAKEUP, triggerAtMillis, pendingIntent);
+        // setAlarmClock: guaranteed delivery on ALL Android versions (no permissions needed)
+        // Does NOT get affected by Doze, Battery Optimization, or Samsung restrictions
+        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.LOLLIPOP) {
+            Intent showIntent = new Intent(context, Helper.getMainActivityClass(context));
+            showIntent.putExtra("ALARM_UID", alarmUid);
+            PendingIntent showPendingIntent;
+            if (android.os.Build.VERSION.SDK_INT >= android.os.Build.VERSION_CODES.S) {
+                showPendingIntent = PendingIntent.getActivity(context, notificationID + 10000, showIntent, PendingIntent.FLAG_MUTABLE);
+            } else {
+                showPendingIntent = PendingIntent.getActivity(context, notificationID + 10000, showIntent, PendingIntent.FLAG_UPDATE_CURRENT);
+            }
+            AlarmManager.AlarmClockInfo alarmClockInfo = new AlarmManager.AlarmClockInfo(triggerAtMillis, showPendingIntent);
+            alarmManager.setAlarmClock(alarmClockInfo, pendingIntent);
+            Log.d(TAG, "Using setAlarmClock (guaranteed delivery, no permissions needed)");
         } else {
-            alarmManager.set(AlarmManager.RTC_WAKEUP, triggerAtMillis, pendingIntent);
+            alarmManager.setExact(AlarmManager.RTC_WAKEUP, triggerAtMillis, pendingIntent);
+            Log.d(TAG, "Using setExact (pre-Lollipop fallback)");
         }
         Log.d(TAG, "SDK version: " + Build.VERSION.SDK_INT);
         Log.d(TAG, "scheduling alarm with notification id: " + notificationID);
diff --git a/android/src/main/java/com/expoalarmmodule/Manager.java b/android/src/main/java/com/expoalarmmodule/Manager.java
index 004075da7464088c26edd8b5e6e2e5a191ec7633..c65a8c756aa605e571daba3e4386a4c33d15a9b2 100644
--- a/android/src/main/java/com/expoalarmmodule/Manager.java
+++ b/android/src/main/java/com/expoalarmmodule/Manager.java
@@ -114,9 +114,11 @@ public class Manager {
     static void start(Context context, String alarmUid) {
         activeAlarmUid = alarmUid;
         sound = new Sound(context);
-        sound.play("default");
-
-        Log.d(TAG, "Starting " + activeAlarmUid);
+        // Use the alarm's custom sound if available
+        Alarm alarm = Storage.getAlarm(context, alarmUid);
+        String soundName = (alarm != null && alarm.sound != null && !alarm.sound.isEmpty()) ? alarm.sound : "default";
+        sound.play(soundName);
+        Log.d(TAG, "Starting " + activeAlarmUid + " with sound: " + soundName);
     }
 
     public static void stop(Context context) {
