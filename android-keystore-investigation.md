# تحقيق بيانات اعتماد Android Keystore

## النتائج حتى الآن

- تؤكد واجهة Expo أن مشروع `@zakariasalah/awafiyat` يحتوي على معرّف تطبيق Android الصحيح: `io.awafiyat.health`.
- يحدد ملف الإنتاج في EAS استخدام بيانات اعتماد بعيدة، وقد أظهر EAS CLI: `Using Keystore from configuration: Google Play Upload Key 2026 (default)`.
- رغم ذلك، تفشل آخر عمليات البناء بخطأ `EAS_BUILD_INVALID_KEYSTORE_ALIAS_ERROR` ورسالة أن الـ alias المحدد غير موجود داخل ملف الـ keystore.
- لا تُظهر واجهة بيانات الاعتماد العامة alias أو خيار تنزيل ملف الـ keystore؛ القائمة المتاحة على عنصر معرّف التطبيق تعرض حذف التكوين فقط، لذلك لم يُجرَ أي تغيير من المتصفح.
- تم تنزيل ملف `Google Play Upload Key 2026` محلياً من خلال مدير بيانات اعتماد EAS وفحصه بـ `keytool`. يحتوي على إدخال واحد واسمه المستعار الفعلي هو `awafiyat-upload-2026`، وهو مطابق تماماً للـ alias المخزّن الذي يعرضه EAS.

## الاستنتاج التشغيلي

الـ alias داخل الملف وبيانات الاعتماد المعروضة من EAS متطابقان. لذلك لا يرجح أن يكون الخطأ ناتجاً عن alias مكتوب بشكل خاطئ، بل من ربط بيانات الاعتماد البعيدة أو من بيانات مخزنة غير متسقة في تكوين EAS. الخطوة التالية هي إعادة ربط ملف الـ keystore نفسه ببيانات الاعتماد البعيدة مع الاسم المستعار المتحقق منه، دون تغيير ملف المفتاح أو إعدادات Android/AdMob.

## قيود الحفاظ على الإعدادات

لن تُعدّل إعدادات AdMob أو معرفات Android أو إعدادات الإعلانات أثناء هذه المعالجة.
