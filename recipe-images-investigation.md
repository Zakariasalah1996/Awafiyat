# تحقيق صور الوصفات

## مصدر الإنتاج

- واجهة الصور العامة: `https://alfafiyat.com/api/recipes/images` (قراءة مباشرة بتاريخ 2026-07-18).
- الاستجابة الحالية تخلط نوعين مختلفين في خريطة واحدة: أسماء صور الفئات المحلية مثل `iraqi-rice` و`fresh-salads`، وروابط HTTP مخصصة.
- من أمثلة الروابط المرفوعة من لوحة التحكم في الاستجابة: `breakfast_1` إلى `breakfast_4` تحت مسار `recipe-images/...`.
- توجد أيضًا روابط HTTP متكررة في بيانات وصفات الخليج والصحة، ولذلك لا يستطيع التطبيق تمييز الصورة المرفوعة من لوحة التحكم عن رابط افتراضي موجود داخل ملف الوصفات بمجرد فحص بادئة `https://`.

## مسار الخادم الحالي

- `GET /api/recipes/images` يملأ الخريطة أولًا بجميع قيم `recipe.image` من ملف الوصفات، ثم يحاول تجاوزها بسجلات جدول `recipe_images` عبر طبقة MySQL.
- خادم Render يستخدم PostgreSQL، لذلك طبقة MySQL لا تُعد مخزنًا موثوقًا لصور لوحة التحكم على الإنتاج.
- `POST /api/admin/upload-image` يرفع الملف، لكنه لا يتلقى `recipeId` من الواجهة الحالية؛ ثم تحفظ اللوحة الرابط في نص الوصفة وتحاول نداء `POST /api/admin/recipe-image` المنفصل.
- تعديل الوصفة يعيد كتابة `lib/data/recipes.ts` داخل حاوية التشغيل بواسطة الحذف ثم الإضافة؛ هذا تخزين مؤقت وغير دائم على Render وقد يغيّر ترتيب الوصفة ويضيع بعد إعادة النشر.

## مسار التطبيق الحالي

- شاشات المكتبة والتفاصيل ومخطط الوجبات تربط الصورة المخصصة باستخدام `recipe.id`، وليس فهرس القائمة، وهذا الجزء صحيح.
- `hooks/use-recipe-images.ts` يخزن كل قيمة HTTP من واجهة الصور في AsyncStorage بالمفتاح `recipe_custom_images_v4`، ولا يميز بين رابط مرفوع من لوحة التحكم ورابط افتراضي موجود داخل الكود.
- النتيجة المحتملة: أي روابط HTTP افتراضية أو مؤقتة يعيدها الخادم تُعامل كتعديلات إدارية وتستبدل الصور المحلية داخل التطبيق بصورة جماعية.

## اتجاه الإصلاح

إنشاء مخزن PostgreSQL مستقل لصور الوصفات المرفوعة فقط، وجعل واجهة `/api/recipes/images` تعيد هذه التعديلات فقط مع `revision` ثابت، وربط الرفع والحفظ بـ `recipeId` في طلب واحد. يجب ألا تُعاد صور الفئات أو روابط الكود الافتراضية عبر واجهة التعديلات، وأن يمسح التطبيق الكاش القديم مرة واحدة عند الانتقال إلى المخطط الجديد.

## السبب الجذري المؤكد

كان المسار العام `/api/recipes/images` يبني خريطة من **كل الوصفات الموجودة في `recipes.ts`** ثم يرسلها إلى الهاتف بوصفها صورًا مخصصة. التطبيق يخزن هذه الخريطة في `AsyncStorage` ويمنحها الأولوية على الصور المحلية؛ لذلك تحولت جميع صور الفئات والصور الافتراضية إلى overrides دائمة، فبدت الصور وكأنها استُبدلت كلها. كما أن لوحة التحكم كانت ترفع الملف أولًا من دون `recipeId`، ثم تحفظ رابطه في طلب ثانٍ وتعيد كتابة حقل `image` داخل ملف الوصفات، وهو ما خلط المحتوى الأساسي بالتعديل الإداري.

## الإصلاح المنفذ

- إنشاء جدول PostgreSQL مستقل `awafiyat_recipe_images` يربط كل `recipeId` برابط HTTPS واحد عبر upsert.
- إرجاع صور لوحة التحكم فقط من `/api/recipes/images`، مع `Cache-Control: no-store`، وعدم إرسال الصور الافتراضية.
- ترحيل غير مدمر للصور القديمة التي رُفعت فعليًا إلى مسار `/recipe-images/`، من دون ترحيل صور Unsplash أو صور الفئات.
- إرسال `recipeId` مع رفع الملف نفسه من لوحة التحكم، وعدم إعادة كتابة حقل الصورة داخل بيانات الوصفة الأساسية.
- ترقية مفتاح كاش الهاتف إلى `recipe_custom_images_v5` ومسح مفاتيح v3/v4 القديمة؛ كما تُعتمد الخريطة الفارغة لإزالة التعديلات القديمة.
- إضافة ثلاثة اختبارات خاصة بالصور ضمن بوابة نجح فيها 27 اختبارًا، مع نجاح TypeScript وESLint بلا أخطاء وتجميع خادم Render وحزمة Android 1.0.78.
