# تحقيق تسجيل الإشعارات في ألف عافيات

## دليل الجهاز

تُظهر شاشة التشخيص المرفقة من المستخدم أن التطبيق المثبّت هو الإصدار `1.0.77`، وأن صلاحية الإشعارات `granted`، وأن عنوان API الفعلي هو `https://alfafiyat.com`. كما نجح الجهاز في استخراج Expo Push Token صالح من الشكل `ExponentPushToken[...]` باستخدام EAS Project ID `ac8a9414-c049-43f6-aa26-4647b61e4d28`، لكن طلب تسجيل الرمز في الخادم أعاد HTTP `500` أثناء استعلام جدول الرموز.

## الخادم الإنتاجي

استجاب [https://alfafiyat.com/api/health](https://alfafiyat.com/api/health) بالقيمة `ok: true` وصرّح بأن قاعدة البيانات الإنتاجية من نوع PostgreSQL. في المقابل، ملف `server/db.ts` الحالي ينشئ اتصالًا عبر `mysql2` ويستخدم مخطط Drizzle من نوع MySQL واستعلامات ذات علامات backtick. لوحة التحكم ومسار `/api/user/push-token` كلاهما يستوردان هذه الطبقة نفسها، وهو عدم توافق معماري مباشر مع قاعدة Render الإنتاجية.

## المخطط والترحيلات

يعرف `drizzle/schema.ts` جدول `push_tokens` بالأعمدة `id`, `userId`, `token`, `platform`, `isActive`, `createdAt`, `updatedAt`. ملفات الترحيل الموجودة مكتوبة بصيغة MySQL؛ ينشئ `0001_broken_terrax.sql` الجدول ويجعل `userId` إلزاميًا، ثم يجعل `0002_dashing_lorna_dane.sql` العمود اختياريًا. لا تمثل هذه الملفات ترحيلات PostgreSQL قابلة للتطبيق مباشرة على Render.

## الاستنتاج الأولي

استخراج الرمز والصلاحية يعملان داخل APK. موضع الفشل هو تسجيل الرمز في قاعدة الخادم، وليس إعداد Expo على الهاتف. يلزم توحيد طبقة البيانات مع PostgreSQL الإنتاجية، أو إضافة طبقة PostgreSQL صريحة لمسارات Render مع ترحيل آمن للأعمدة والجداول قبل اختبار إرسال الإشعارات من لوحة التحكم.
