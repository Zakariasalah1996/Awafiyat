# تدقيق iOS وRevenueCat — النسخة 1.0.83

## النتيجة الرئيسية

سبب تقني مباشر محتمل جداً لفشل الاشتراكات على iOS موجود في المصدر الحالي: ملف `lib/subscription-context.tsx` يمرر مفتاح RevenueCat العام الخاص بـGoogle (`goog_…`) لكل المنصات الأصلية، ولا يوجد أي مفتاح Apple عام (`appl_…`) في المشروع. لذلك يتهيأ RevenueCat على iOS بمشروع/تطبيق Google بدلاً من تطبيق App Store، ولن يتمكن من إرجاع منتجات StoreKit الصحيحة للحزمة `io.awafiyat.health`.

## نتائج التدقيق

| المجال | الوضع الحالي | الإجراء المطلوب |
|---|---|---|
| مفتاح RevenueCat حسب المنصة | مفتاح Google ثابت في `subscription-context.tsx` لكل من Android وiOS. | اختيار مفتاح Apple العام على iOS ومفتاح Google العام على Android، مع إبقائهما قابلين للتهيئة الآمنة. |
| Offering | الخطاف يبحث أولاً عن `rc_monthly$` ثم `offerings.current`. | الاعتماد على Current Offering أولاً أو Offering معرّف موثق، والتحقق من وجود الشهري والسنوي برسالة خطأ مفهومة. |
| Entitlement | المعرّف ثابت `premium`. | إبقاؤه إذا كان مطابقاً للوحة RevenueCat على Apple وGoogle؛ يلزم التحقق من الحساب. |
| Product IDs | لا توجد معرفات Apple موثقة أو اختبارات تطابق داخل المشروع. | استخراجها من App Store Connect/RevenueCat والتحقق من ربطها بالـOffering نفسه. |
| تجربة 3 أيام | الواجهة تعد بها دائماً بصرف النظر عن بيانات المنتج الفعلية. | عرض التجربة فقط إذا أثبت StoreKit/RevenueCat أن المنتج يحمل عرضاً تمهيدياً؛ وإلا عرض زر اشتراك عادي. |
| السعر والمدة | السعر يأتي من `priceString`، وهذا صحيح من حيث المصدر؛ لكن السعر الشهري المحسوب للسنوي يُعرض بلا تنسيق عملة محلي. | عرض السعر المحلي الرسمي من StoreKit، وتجنب تركيب قيمة غير محلية. |
| الإلغاء | النص يقول «إعدادات Google Play» على iOS أيضاً. | نص خاص بالمنصة وروابط إدارة الاشتراك/Apple. |
| الخصوصية وEULA | لا توجد روابط قابلة للنقر في شاشة الاشتراك أو نافذة التأكيد. | إضافة روابط وظيفية قبل زر الشراء وبالقرب من الإفصاح التجديدي. |
| الاستعادة | تستدعي `Purchases.restorePurchases()`، لكن نجاحها يُعلن دائماً حتى إن لم يوجد استحقاق. | إرجاع نتيجة واضحة وتمييز «تمت الاستعادة» عن «لا توجد مشتريات قابلة للاستعادة». |
| الخطأ | `purchasePackage` يبتلع خطأ الشراء ويعيد `false`، فتغلق النافذة بلا تفسير للمستخدم. | إرجاع نتيجة منظمة: نجاح/إلغاء/فشل، وعرض رسالة مراجعة واضحة دون كشف تفاصيل حساسة. |
| هوية RevenueCat | لا يُمرر `appUserID` عند التهيئة. | مقبول للمستخدم المجهول، لكن ينبغي تثبيت سياسة الهوية وتوثيقها لتفادي فقدان الربط؛ ليس سبب الرفض الأساسي. |
| وضع الصوت في الخلفية | `expo-video` مضبوط على `supportsBackgroundPlayback: true`. | ضبطه إلى `false` وإزالة وضع `audio` الناتج ما دام التطبيق لا يشغّل صوتاً مستمراً في الخلفية. |
| iPad | `supportsTablet: true` والشاشة تستخدم بطاقتين أفقيتين ومودال سفلي دون حد أقصى للعرض. | إضافة تخطيط متجاوب وحد أقصى للعرض، مع إغلاق واضح واختبارات وحدات للحالات الحرجة. |

## ملفات الإصلاح الأساسية

- `lib/subscription-context.tsx`
- `hooks/use-subscriptions.ts`
- `app/(tabs)/subscription.tsx`
- `app.config.ts`
- `app/sections/about.tsx`
- `legal/terms-of-service.html`
- اختبارات الإصدار داخل `__tests__/`

## قيد حسابي قبل اكتمال الإصلاح

يلزم الحصول على **RevenueCat Apple public SDK key** المرتبط بتطبيق iOS `io.awafiyat.health`، والتحقق من Current Offering والمنتجات والـEntitlement في RevenueCat وApp Store Connect. هذه إجراءات قراءة/تعديل حسابية حساسة، لذلك لا تُنفذ ولا تُحفظ أي تغييرات في الحساب دون موافقة المستخدم قبل تلك المرحلة.

## تحقق RevenueCat من الحساب — قراءة فقط

تم فتح مشروع RevenueCat **AFIYAT LTD** ذي المعرّف `805827ed` في جلسة المستخدم. تعرض الصفحة الرئيسية معاملات حديثة من **Play Store** فقط، ولا يظهر فيها حتى الآن دليل على معاملات أو منتجات Apple. يؤكد ذلك أن تكامل Android يعمل تجارياً، لكنه لا يثبت وجود تطبيق App Store أو Products/Offering صالح لـiOS. لم يُعدّل أي إعداد في الحساب.

- مسار قسم التطبيقات في RevenueCat هو `https://app.revenuecat.com/projects/805827ed/apps`. أمكن فتحه للقراءة فقط، وكانت بيانات المحتوى ما تزال قيد التحميل عند اللقطة الأولى؛ لم يُجر أي تعديل.

## تطبيق Apple في RevenueCat

تبيّن أن المشروع يحتوي تطبيقين بالحزمة نفسها `io.awafiyat.health`: تطبيق Android باسم **ألف عافيات** ومعرّف `app859794b99e`، وتطبيق Apple باسم **ألف عافيات أبل** ومعرّف `app9563a53372`. مفتاح Apple العام المتاح للـSDK هو `appl_biJjDFnvutsNdCynbpWLEXLRMLP`. هذا مفتاح SDK عام مخصص للتضمين في التطبيق، ويجب اختياره فقط عندما تكون `Platform.OS === "ios"` مع إبقاء مفتاح Google الحالي لـAndroid. لم يُغيّر أي إعداد في RevenueCat.

## Offerings في RevenueCat

تعرض صفحة `https://app.revenuecat.com/projects/805827ed/product-catalog/offerings` ثلاثة Offerings نشطة: `default` وفيه حزمتان، و`rc_monthly$` وهو الـCurrent Offering بعلامة الاختيار وفيه حزمتان، و`rc_annual$` وفيه حزمة واحدة. يتطابق ذلك جزئياً مع الخطاف الحالي الذي يختار `rc_monthly$`، لكن يلزم فتحه والتحقق من أن حزمتَيه تحتويان منتجات Apple، لا منتجات Google فقط.

تم فتح الـCurrent Offering `rc_monthly$` للقراءة فقط، ومعرّفه الداخلي `ofrng6fc491f6dd`. كانت تفاصيل الحزم قيد التحميل عند اللقطة الأولى، ولم يُجر أي تعديل.

## السبب المؤكد لفشل المنتجات على iOS

الـCurrent Offering `rc_monthly$` يحتوي حزمتين فقط وكلتاهما مرتبطة بمنتجات Google Play ذات معرفات base plan: `io.awafiyat.health.yearly:yearly-plan` و`io.awafiyat.health.monthly:monthly-plan`. وفي صفحة Products يظهر قسم Android به المنتجين، بينما قسم تطبيق Apple **ألف عافيات أبل** فارغ تماماً ولا يحتوي أي Product. بناءً على ذلك، حتى بعد استخدام مفتاح `appl_…` الصحيح لن يعرض StoreKit منتجات حتى تُنشأ أو تُستورد اشتراكات App Store Connect وتُربط بحزم الـOffering والـEntitlement. لم يُجر أي تعديل في الحساب.

## حالة App Store Connect — قراءة فقط

يظهر تطبيق **ألف عافيات** في حساب AFIYAT LTD، والإصدار الموجود في App Store Connect هو `iOS 1.0.61` وحالته **Rejected**. كما تعرض المنصة تنبيهاً عاماً لمراجعة أسئلة التصنيف العمري الجديدة المتعلقة بقدرات التواصل الاجتماعي داخل قسم App Information. لم يُجر أي تعديل أو إرسال.

## صفحة الإصدار المرفوض في App Store Connect

معرّف التطبيق في App Store Connect هو `6775841115`. صفحة الإصدار تعرض `iOS 1.0.61` بحالة Rejected والبناء المرفق يحمل الإصدار `1.0.0` والبناء `1`. توجد سبع لقطات iPhone في مقاس 6.5 بوصة. قسم **In-App Purchases and Subscriptions** لا يعرض أي عنصر مرفق بالإصدار، بل يعرض تنبيه Apple بأن أول اشتراك يجب تقديمه مع إصدار جديد. هذا ينسجم مع سبب الرفض 2.1. لم يُعدّل أي حقل ولم يُضغط Save أو Update Review.

## اشتراكات App Store Connect

يحتوي App Store Connect فعلياً على مجموعة اشتراكات تلقائية التجدد باسم `Premium` وبداخلها **اشتراكان**. فترة السماح بالفوترة 3 أيام ومفعلة لبيئتي Production وSandbox، وخيار Streamlined Purchasing مفعّل. المشكلة ليست غياب المنتجات من Apple بالكامل؛ بل أن منتجات Apple غير مستوردة وغير مربوطة داخل RevenueCat، كما أنها غير مرفقة حالياً بإصدار 1.0.61. لم يُجر أي تعديل.

### المنتج السنوي في Apple

المنتج السنوي موجود بمعرّف Apple `6775909097` ومعرّف المنتج `io.awafiyat.health.yearly`، مدته سنة وحالته **Waiting for Review** داخل مجموعة Premium. حقل صورة الاشتراك الاختيارية يظهر بلا صورة في القراءة الحالية. ملاحظة المراجعة الحالية تقول إن الاشتراك يفتح حاسبة السعرات والوصفات المخصصة والتنبيهات الصحية وأدوات إدارة الأسرة؛ يجب التحقق من تطابق هذه الصياغة حرفياً مع وظائف النسخة الحالية لتجنب تضليل المراجع. لم يُجر أي تعديل.

بيانات توطين السنوي: الاسم `الاشتراك السنوي` والوصف `وصول كامل لجميع ميزات تطبيقنا ألف عافيات`. لا تظهر صورة 1024×1024 مخصصة للاشتراك في حقل Image. صورة **Review Information** الحالية لقطة شاشة صغيرة لواجهة الشراء وتحتوي أسعاراً؛ تصلح كمرجع للمراجع، لكنها ليست صورة App Store Promotion مستقلة، وهو مطابق لملاحظة Apple في البند 2.3.2. الأسعار نفسها مضبوطة عبر Apple وتظهر في صفحة All Prices and Currencies، لكن يجب ألا تُكتب يدوياً داخل الصورة الترويجية.

### المنتج الشهري في Apple

المنتج الشهري موجود بمعرّف Apple `6775843873` ومعرّف المنتج `io.awafiyat.health.monthly`، مدته شهر وحالته **Waiting for Review**. توطينه العربي: `الاشتراك الشهري` ووصفه `وصول كامل لجميع ميزات تطبيقنا ألف عافيات`. لا تظهر صورة 1024×1024 مخصصة للاشتراك، وملاحظة المراجعة مطابقة لملاحظة السنوي. المنتج متاح في جميع البلدان أو المناطق المحددة. لم يُجر أي تعديل.

## استحقاقات RevenueCat

توجد استحقاقات نشطة اثنان: `AFIYAT LTD Pro` و`premium`، ويرتبط كل منهما بمنتجين. المصدر الحالي يتحقق من `premium`، لذا يلزم إبقاؤه مرجع الوصول الوحيد في التطبيق والتحقق من المنتجات المرتبطة به. وجود استحقاقين متوازيين قد يسبب التباساً في الإعداد، لكنه لا يثبت وحده وجود خطأ. لم يُجر أي تعديل.

تفاصيل استحقاق `premium` تؤكد أن المنتجين المرتبطين به هما Android فقط: `io.awafiyat.health.monthly:monthly-plan` و`io.awafiyat.health.yearly:yearly-plan` تحت تطبيق «ألف عافيات» ذي أيقونة Android. لا يرتبط به حالياً المنتجان Apple `io.awafiyat.health.monthly` و`io.awafiyat.health.yearly`. لذلك يلزم، في مرحلة إعداد الحساب وبموافقة المستخدم، استيراد منتجي Apple إلى RevenueCat ثم ربطهما باستحقاق `premium` وحزم الـCurrent Offering؛ وإلا لن يظهر أي اشتراك صالح على iOS حتى بعد تصحيح مفتاح SDK.

## معلومات App Store العامة — 20 يوليو 2026

المصدر: https://appstoreconnect.apple.com/apps/6775841115/distribution/info

تؤكد الصفحة أن معرّف الحزمة هو `io.awafiyat.health`، واللغة الأساسية العربية، والفئة الأساسية **Health & Fitness**، والإصدار المعروض `1.0.61` بحالة **Rejected**. اتفاقية الترخيص الحالية هي **Apple’s Standard License Agreement**، وهو أمر صالح لاستخدام رابط EULA القياسي من Apple في الوصف وشاشة الاشتراك. كما أن التطبيق معلن أنه ليس جهازاً طبياً منظماً. لا يظهر رابط سياسة الخصوصية في صفحة App Information العامة؛ يلزم قراءته من صفحة الإصدار/التوطين قبل اعتماد رابط داخل التطبيق. لم يُحفظ أو يُعدّل أي حقل.

## رابط الخصوصية في صفحة الإصدار

جرى فتح صفحة الإصدار المرفوض `1.0.61` للقراءة فقط، لكن محتوى الإصدار بقي في حالة تحميل ولم تعرض الواجهة حقول التوطين أو رابط سياسة الخصوصية. لذلك لن نعتمد رابطاً غير متحقق منه من App Store Connect. كما أثبت اختبار HTTP أن المسارين المقترحين `https://alfafiyat.com/legal/privacy-policy.html` و`https://alfafiyat.com/legal/terms-of-service.html` يعيدان 404 حالياً؛ يجب نشر الصفحات قانونياً على نطاق رسمي أو اعتماد رابط صحيح موجود قبل بناء المرشح النهائي. لم يُعدّل أي حقل في App Store Connect.

## مفتاح RevenueCat العام لتطبيق Apple

جرى التحقق قراءةً فقط من صفحة Apps في RevenueCat بتاريخ 20 يوليو 2026. تطبيق Apple المسمى «ألف عافيات أبل» ومعرّف الحزمة `io.awafiyat.health` يملك مفتاح SDK عاماً يبدأ بـ`appl_` ومختلفاً عن مفتاح Android الذي يبدأ بـ`goog_`. يجب اختيار مفتاح Apple على iOS ومفتاح Google على Android داخل `SubscriptionProvider`. هذه مفاتيح SDK عامة صممت للتضمين في التطبيق وليست مفاتيح API سرية. لم يُعدّل أي إعداد في RevenueCat.

## تحديث RevenueCat — 20 يوليو 2026

- أُنشئ منتج Apple السنوي بالمعرّف `io.awafiyat.health.yearly` والاسم `Apple Annual Premium` والنوع `Subscription` من داخل استحقاق `premium`.
- ظهرت رسالة نجاح RevenueCat: `Products attached successfully`، ما يؤكد إرفاق المنتج بالاستحقاق عند الإنشاء.
- لم تُجرَ أي عملية حذف أو فصل للمنتجات أو الاستحقاقات القائمة.

أُعيد فتح صفحة الاستحقاق بعد الحفظ، وظهر `Apple Annual Premium` فعلياً في الجدول تحت تطبيق «ألف عافيات أبل» بالمعرّف `io.awafiyat.health.yearly` إلى جانب المنتج الشهري Apple؛ وبذلك أصبح الاستحقاق `premium` مرتبطاً بمنتجي Apple ومنتجي Google معاً.

أُعيد التحقق من الـCurrent Offering `rc_monthly$` (المعرّف الداخلي `ofrng6fc491f6dd`) بعد إكمال الاستحقاق. ما تزال حزمة `$rc_annual` تعرض منتج Google السنوي فقط، وحزمة `$rc_monthly` تعرض منتج Google الشهري فقط؛ لذا يلزم إرفاق منتج Apple المناظر بكل حزمة مع الحفاظ على روابط Google الحالية.

أظهر فحص واجهة الـOffering أن النقر على اسم المنتج داخل بطاقة الحزمة يفتح صفحة منتج Google نفسه ولا يفتح إدارة الحزمة؛ لم يُجرَ أي تعديل. سنستخدم عنصر إدارة الحزمة أو قائمة إجراءاتها بدلاً من رابط المنتج.

فحص HTML المحمّل للـOffering لم يُظهر زرًا نصيًا ثابتًا من نوع `Add product` أو `Edit package` داخل بطاقة الحزمة؛ الظاهر الثابت هو رابط المنتج فقط. يرجّح أن إدارة الحزمة تظهر من عنصر تفاعلي عند المرور أو من قائمة إجراءات مرئية عند موضع محدد، ولذلك لم يُحفظ أي تعديل بعد.

لم يُظهر المرور فوق الحافة اليمنى لبطاقة `$rc_annual` أي قائمة أو زر إضافي، كما لم يظهر عنصر تفاعلي مستقل لمساحة البطاقة في لقطة المتصفح. لم يحدث أي تغيير في الحزمة؛ الخطوة التالية هي فحص زر `Edit` العام للـOffering لمعرفة إن كان يتيح إدارة الحزم.

كشف زر `Edit` العام للـOffering محرر الحزم الكامل. في حزمة `$rc_annual` يظهر منتج Android الحالي `سنوي` محفوظاً، بينما حقل تطبيق Apple «ألف عافيات أبل» مضبوط على `No product`. وتظهر حزمة `$rc_monthly` أسفلها بالإعداد نفسه. هذا هو المسار الصحيح لإضافة منتج Apple واحد لكل حزمة مع إبقاء منتج Android دون تغيير.

داخل محرر `https://app.revenuecat.com/projects/805827ed/product-catalog/offerings/ofrng6fc491f6dd/edit` تم اختيار `Apple Annual Premium` (`io.awafiyat.health.yearly`) في حقل تطبيق Apple لحزمة `$rc_annual`. منتج Android السنوي بقي كما هو، ولم يُضغط زر `Save` بعد ريثما يضاف المنتج الشهري المناظر في العملية نفسها.

فُتحت حزمة `$rc_monthly` داخل محرر الـOffering. تم التأكد من بقاء Test Store على `No product` ومنتج Android على `شهري` دون تغيير، ويظهر حقل تطبيق Apple «ألف عافيات أبل» على `No product` وجاهز لاختيار `Apple Monthly Premium`.

اكتمل اختيار `Apple Monthly Premium` (`io.awafiyat.health.monthly`) في حقل Apple لحزمة `$rc_monthly`. أصبح النموذج الآن يحتوي Apple Annual للحزمة السنوية وApple Monthly للحزمة الشهرية، مع بقاء منتجي Android الحاليين وTest Store كما هما. لم يُضغط `Save` بعد في هذه اللحظة.

حُفظ تعديل الـCurrent Offering بنجاح، وظهرت رسالة RevenueCat: `Offering "شهري (rc_monthly$)" was updated.` بتاريخ 2026-07-20. يتبقى التحقق التفصيلي من استمرار الاختيارين داخل شاشة التحرير ثم الانتقال إلى تحديث بيانات App Store Connect.

أُعيد فتح محرر الـOffering بعد الحفظ للتحقق من الاستمرارية. تظهر الحزمة السنوية قابلة للتوسيع وTest Store ما يزال `No product`؛ لم يُجرَ أي تعديل جديد خلال التحقق.

اكتمل التحقق بعد إعادة فتح محرر الـOffering: حزمة `$rc_annual` تعرض `Apple Annual Premium` مع Android `سنوي` وTest Store=`No product`، وحزمة `$rc_monthly` تعرض `Apple Monthly Premium` مع Android `شهري` وTest Store=`No product`. بذلك تم ربط منتجي Apple بالحزمتين بنجاح مع الحفاظ على روابط Google الحالية.

فُحصت صورتا الاشتراك الشهرية والسنوية بصرياً: كلتاهما PNG مربعة 1024×1024، أصل فني مستقل وليستا لقطتي شاشة، متمايزتان بوضوح (تقويم شهري مقابل رزمة تقاويم سنوية)، ولا تتضمنان أسعاراً أو عملات أو نصاً ترويجياً مضللاً.

نتائج فحوص مرشح iOS: TypeScript ناجح، ESLint ناجح، 13 ملف اختبار ناجحاً مع 81 اختباراً ناجحاً وملفين/اختبارين متجاوزين عمداً، وتجميع Expo لمنصة iOS ناجح مع إنتاج حزمة Hermes بحجم 7.2 MB. كما نجح اختبار جاهزية Apple المحدّث 6/6 ويغطي ربط الشراء والاستعادة والإغلاق والعودة وتخطيط iPad.

مراجعة App Store Connect السلبية للاشتراك السنوي: المنتج `io.awafiyat.health.yearly` موجود في مجموعة Premium، مدته سنة، حالته Waiting for Review، متاح في جميع البلدان/المناطق، والترجمة العربية الحالية «الاشتراك السنوي» مع وصف بلا سعر. توجد لقطة مراجعة تعرض واجهة الاشتراك، بينما الصورة الترويجية الاختيارية المعدّة محلياً لم تُثبت بعد على الصفحة. لم يُحفظ أو يُرسل أي تعديل.

مراجعة App Store Connect السلبية للاشتراك الشهري: المنتج `io.awafiyat.health.monthly` موجود في مجموعة Premium، مدته شهر، حالته Waiting for Review، متاح في جميع البلدان/المناطق، والترجمة العربية الحالية «الاشتراك الشهري» مع وصف بلا سعر. توجد لقطة مراجعة وفق بنية المنتج السنوي، والصورة الترويجية الاختيارية المعدّة محلياً لم تُثبت بعد على الصفحة. لم يُحفظ أو يُرسل أي تعديل.

حالة Business في App Store Connect بتاريخ 2026-07-20: Paid Apps Agreement نشط حتى 2027-05-21، Free Apps Agreement نشط، الحساب البنكي AFIYAT LTD نشط، نماذج الضرائب الأمريكية نشطة، ومتطلبات الامتثال المعروضة نشطة. لا توجد اتفاقية معلّقة، ولم يُجرَ أي تعديل.


## مراجعة نسخة App Store Connect المرفوضة — 20 يوليو 2026

النسخة الحالية في App Store Connect هي `1.0.61` وحالتها **Rejected**، والبناء المرتبط هو build `1` بإصدار ثنائي `1.0.0`؛ لذلك يجب استبداله ببناء iOS جديد يضم إصلاحات StoreKit الحالية. تعرض الصفحة أسباب الرفض 2.1.0 و2.3.2 و2.5.4 و3.1.2، ولم يُستخدم زر `Update Review`.

بيانات App Privacy منشورة، ورابطها `https://www.afiyatltd.co.uk/privacy` يعمل. اتفاقية Paid Apps نشطة، والضرائب والخدمات المصرفية مكتملة، وApp Store يستخدم Standard License Agreement. منتجا الاشتراك الشهري والسنوي حالتهما `Developer Action Needed`، لكن الترجمة والتسعير وصورة المراجعة موجودة لكل منهما، وقد رُبطا في RevenueCat باستحقاق `premium` والـCurrent Offering.

تبين أن رابطَي التطبيق السابقين `https://alfafiyat.com/?legal=privacy` و`?legal=terms` يفتحان الصفحة الرئيسية في الإنتاج؛ استُبدلا داخل شاشة الاشتراك بصفحتي الشركة العاملتين `/privacy` و`/terms`، ونجح اختبار Apple 6/6 وفحص TypeScript. صفحة الإصدار تحتوي سبع لقطات iPhone من أصل عشر، ولم يُحفظ أو يُرسل أي تعديل في App Store Connect.

- حقول نسخة `1.0.61` الظاهرة حالياً: Support URL = `https://www.afiyatltd.co.uk`، Marketing URL فارغ، Copyright = `AFIYAT LTD`، والنص الترويجي `تطبيق مميز لمطبخ مرتب و ذكي`. لم يُعدّل أي حقل.

يعرض الإصدار المرفوض البناء القديم `1 (1.0.0)` فقط ولا توجد اشتراكات مرفقة ضمن قسم In-App Purchases and Subscriptions. شاشة الإصدار توفر `Update Review`، لكنه لم يُستخدم، ولم تُحفظ أي تغييرات أو تُرسل للمراجعة.

## نص رفض Apple الدقيق وخريطة المعالجة

| البند | ملاحظة المراجع | حالة المعالجة |
|---|---|---|
| 2.3.2 | صورة الـPromoted IAP كانت لقطة من التطبيق وتضمنت أسعاراً للمنتجات الشهري والسنوي. | أُنشئت صورتان فريدتان 1024×1024 بلا أسعار أو لقطات واجهة، وتنتظران موافقة المستخدم لرفعهما إلى حقلي صور الترويج في App Store Connect. |
| 2.1(b) | التطبيق لم يستخدم StoreKit للاشتراك على iPad Air 11-inch (M3)، iPadOS 26.5. | صُحح مفتاح RevenueCat حسب المنصة، واستُورد وربط منتجا Apple بالاستحقاق `premium` والـCurrent Offering، وتحسن مسار الشراء والاستعادة والأخطاء، ونجحت الاختبارات. يلزم بناء iOS جديد واختبار Sandbox عملي. |
| 2.5.4 | `UIBackgroundModes` يعلن `audio` من دون تشغيل صوت خلفي. | أُزيل وضع الصوت الخلفي من إعدادات iOS، وأثبت اختبار الجاهزية عدم وجوده. |
| 3.1.2(c) | غاب رابط Terms of Use (EULA) العامل من App Store metadata؛ ومع Standard EULA يجب وضع الرابط في App Description. | شاشة الاشتراك تستخدم رابط Apple Standard EULA، لكن يجب إضافة الرابط نفسه إلى وصف App Store بعد موافقة المستخدم: `https://www.apple.com/legal/internet-services/itunes/dev/stdeula/`. |

المصدر: رسالة Apple المؤرخة 5 يونيو 2026، Submission ID `ad084e66-5ce4-40da-b929-ae0bdbec477a`. لم يُرسل رد ولم يُستخدم `Resubmit to App Review`.

قبل تعديل الوصف، كانت النسخة العربية تحتوي 7 لقطات iPhone ونصاً ترويجياً قائماً، ولم يتضمن السجل أي تعديل على هذه العناصر. التغيير المصرح به سيقتصر على إلحاق رابط Apple Standard EULA بالوصف وحفظه.

تم استخراج الوصف العربي الحالي من HTML المحفوظ قبل تحريره، وسيُحافظ عليه كاملاً مع إلحاق رابط Apple Standard EULA فقط. لم يُحفظ أي تغيير في App Store Connect حتى هذه النقطة.
تم تحديد موضع حقل الوصف، وما زال عداد الوصف 3,009 أحرف متبقية؛ لا يوجد تغيير محفوظ بعد.
البحث النصي حدّد قسم Description لكنه لم يغيّر الحقل أو يحفظ شيئاً؛ تم إبقاء الصفحة دون تعديل أثناء الانتقال.
