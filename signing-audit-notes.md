# تدقيق مفتاح رفع Android

## الشهادة المؤكدة من الحزمة القديمة

تم فحص `/home/ubuntu/upload/--v1_0_59.aab` بواسطة `keytool -printcert -jarfile`، وكانت النتيجة:

| الحقل | القيمة |
|---|---|
| Owner / Issuer | `CN=Manus App` |
| SHA-1 | `86:DE:A0:84:E6:CA:71:27:D7:5E:3B:F8:90:9A:38:1D:40:E4:0F:7C` |
| SHA-256 | `17:3B:4B:EC:3C:C7:F0:CD:A1:C5:E4:C1:CB:B0:17:01:F1:50:CB:E2:DD:32:6A:C8:96:0D:66:34:71:15:1C:26` |
| الصلاحية | 2026-04-15 إلى 2126-03-22 |

إذن الحزمة المقبولة سابقاً موقعة فعلاً بالمفتاح الذي يطلبه Google Play.

## نتيجة جلسة EAS المحلية

تم تشغيل EAS CLI الحالي، لكن جلسة الطرفية الحالية غير مسجلة الدخول (`Not logged in`). لا يعني ذلك أن المفتاح غير موجود على خادم EAS؛ يلزم الوصول إلى حساب Expo المرتبط بالمشروع لفحص بيانات الاعتماد البعيدة.

## مرجع Expo الرسمي

وفق وثائق Expo الرسمية، يُوقّع EAS Build الحزمة بمخزن المفاتيح المرتبط بالتطبيق. يمكن تنزيل بيانات الاعتماد عبر `eas credentials`، وتصدير الشهادة العامة إلى PEM بواسطة `keytool`. إذا فُقد مخزن المفتاح، يمكن طلب إعادة تعيين Upload Key لدى Google مع بقاء مفتاح توقيع التطبيق لدى Google دون تغيير.

المصادر:

1. https://docs.expo.dev/app-signing/app-credentials/
2. https://docs.expo.dev/app-signing/local-credentials/

## حالة الوصول عبر المتصفح

فتح `https://expo.dev/accounts` أعاد صفحة 404، مع ظهور رابط **Dashboard** في الواجهة؛ يلزم فتح لوحة Expo الصحيحة والتحقق من جلسة الحساب.

## مشروع Expo الأول الظاهر في الحساب

تم فتح مشروع **Awafiyat** المرتبط بالمستودع `Zakariasalah1996/Awafiyat`، ومساره في Expo هو:

`@zakariasalah/httpsmanusimapp-previewmmvdqjehvxpvsqf4pakpm2sessionidhor5ofixjjwjyd6xcd7dku`

تظهر في واجهته عمليات بناء iOS حديثة فقط ولا يظهر ضمن الصفحة الرئيسية سجل بناء Android للإصدار 59/61، لذلك يلزم فحص مشروع الحساب الثاني **expo** الذي تظهر عليه آخر عملية بناء Android.

## محاولة فتح المشروع الثاني

المسار المباشر `@zakariasalah/expo` أعاد **Project not found**. اسم `Zakariasalah1996/expo` الظاهر على البطاقة هو رابط مستودع GitHub وليس بالضرورة slug المشروع في Expo. سيتم استخراج رابط المشروع الحقيقي من صفحة الحساب المحفوظة بدلاً من التخمين.

## مشروع EAS الصحيح المرتبط بالتطبيق

تم تحديد المشروع الثاني الصحيح: `@zakariasalah/awafiyat`، وهو مرتبط بالمستودع `Zakariasalah1996/expo`. هذا يتوافق مع `appSlug: awafiyat` في إعداد التطبيق.

صفحة البناء الحالية تعرض بناء Android داخلياً حديثاً فقط: `1.0.0 (1)` بملف `preview` منذ 16 يوماً، وتعرض عدة عمليات iOS. لم يظهر بعد بناء الإصدار 59 أو 61؛ يلزم تحميل السجل الأقدم وفحص صفحة Credentials للمشروع نفسه.

## بيانات اعتماد Android الظاهرة في مشروع `@zakariasalah/awafiyat`

لحزمة `io.awafiyat.health` يوجد تكوين افتراضي واحد باسم **Build Credentials jV3TkDMudc**. مخزن المفتاح من نوع JKS، والاسم المستعار للمفتاح:

`f69538afabd38ceeaed7d52071b15003`

تم رفعه في **2 يونيو 2026، 11:27 صباحاً**. واجهة Expo تعرض البصمات مختصرة:

| البصمة | القيمة المختصرة |
|---|---|
| MD5 | `04:82...B7:56` |
| SHA-1 | `51:5A...1C:31` |
| SHA-256 | `C7:88...D7:9D` |

هذه البداية والنهاية لا تطابق المفتاح القديم المطلوب `86:DE...0F:7C` ولا المفتاح الذي وقّع الحزمة المرفوضة `63:E1...6B:F2`. يلزم استخراج القيمة الكاملة والتحقق مما إذا كانت هناك بيانات اعتماد أخرى محفوظة في القائمة.

المصدر: https://expo.dev/accounts/zakariasalah/projects/awafiyat/credentials/android/io.awafiyat.health

## نتيجة قائمة بيانات اعتماد البناء

قائمة Build Credentials لحزمة `io.awafiyat.health` تحتوي **بيانات اعتماد واحدة فقط**: `jV3TkDMudc` (Default)، ولا يظهر فيها تكوين قديم آخر يمكن اختياره مباشرة. بناءً على البصمة المختصرة، هذا المفتاح ليس المفتاح التاريخي `86:DE...0F:7C`.

## فحص مشروع Expo الآخر `Awafiat`

مشروع `@zakariasalah/httpsmanusimapp-previewmmvdqjehvxpvsqf4pakpm2sessionidhor5ofixjjwjyd6xcd7dku` المرتبط بالمستودع `Zakariasalah1996/Awafiyat` لا يحتوي حالياً أي Android Application Identifier أو Android credentials؛ تظهر واجهة **Get started** فقط. لذلك لا توجد فيه نسخة EAS مخزنة من مفتاح الإصدار 59/61.

أعيد فتح صفحة بيانات الاعتماد وتأكد أن الحالة لم تتغير: تكوين واحد فقط `jV3TkDMudc`، ولا يظهر مفتاح تاريخي ثانٍ.

خيارات بيانات الاعتماد الحالية في واجهة EAS هي **Edit** و**Delete** فقط؛ لا يظهر خيار تنزيل مباشر من هذه الصفحة. لم يتم تنفيذ أي تعديل أو حذف.

## Upload Key البديل المُنشأ بعد إثبات فقدان المفتاح القديم

تم إنشاء مفتاح رفع مستقل مملوك لتطبيق ألف عافيات، منفصل عن مفتاح Manus الداخلي وعن مخزن EAS الحالي.

| الحقل | القيمة |
|---|---|
| Package | `io.awafiyat.health` |
| Alias | `awafiyat-upload-2026` |
| الخوارزمية | RSA 4096 / SHA256withRSA |
| SHA-1 | `8D:10:76:F8:84:31:F2:6C:B5:0E:4B:50:F1:AE:9A:3A:F7:9C:34:A5` |
| SHA-256 | `2F:36:A5:FF:0B:70:97:E6:F0:28:72:2A:DC:99:BC:14:9A:B8:64:7D:0D:83:DB:87:6B:BB:24:54:62:8F:F3:A3` |
| SHA-256 للنسخة المشفرة | `8c8d023b232e600206e55aca28042b252c36868821ec56571a4fba4799c08c62` |

تم تصدير شهادة PEM العامة المطلوبة لإعادة تعيين مفتاح الرفع، والتحقق من أن الأرشيف المشفر يُفك بنجاح ويحتوي JKS والشهادة وملف بيانات الاعتماد والبيان. الملفات الخاصة محفوظة خارج مستودع المشروع بصلاحيات `600`، ولم تُضف إلى Git أو حزمة التطبيق.

## Google Play Console

تم فتح حساب المؤسسة **AFIYAT LTD** (معرّف المطور `5527675669406446893`) والعثور على تطبيق واحد باسم **ألف عافيات** بالحزمة `io.awafiyat.health` وحالة الإنتاج. لم يتم إرسال أو تعديل أي إعداد بعد.

تعذّر فتح مسار `app-integrity` مباشرة وأعاد Play Console إلى قائمة التطبيقات، لذا سيُستخدم التنقل الداخلي من لوحة تطبيق ألف عافيات للوصول إلى قسم توقيع التطبيق دون تخمين المسارات.

تم الوصول إلى المسار الصحيح داخل تطبيق ألف عافيات: **محمي من خلال Google Play** (`/protect-with-play`). تعرض الصفحة مجموعة **الحماية في متجر Play** وفيها ست خدمات مفعّلة من سبع، وسيُفتح هذا القسم للعثور على إدارة توقيع التطبيق ومفتاح الرفع. لم يُنفذ أي تغيير.

## صفحة توقيع التطبيق في Google Play

تم الوصول إلى صفحة **توقيع التطبيق** (`/keymanagement`) والتحقق من القيم التالية:

| الشهادة | SHA-1 | SHA-256 |
|---|---|---|
| مفتاح توقيع التطبيق الذي تحتفظ به Google | `7B:32:12:52:46:60:9E:C1:8A:ED:FC:4B:0E:BA:6E:5C:A3:13:CB:51` | `D8:37:80:90:11:0A:EE:EF:5F:62:23:CB:D1:81:73:EE:46:AC:8F:6A:B5:75:4B:98:B4:9B:35:30:DB:27:A7:C1` |
| مفتاح الرفع القديم المفقود | `86:DE:A0:84:E6:CA:71:27:D7:5E:3B:F8:90:9A:38:1D:40:E4:0F:7C` | `17:3B:4B:EC:3C:C7:F0:CD:A1:C5:E4:C1:CB:B0:17:01:F1:50:CB:E2:DD:32:6A:C8:96:0D:66:34:71:15:1C:26` |
| مفتاح الرفع البديل الجديد | `8D:10:76:F8:84:31:F2:6C:B5:0E:4B:50:F1:AE:9A:3A:F7:9C:34:A5` | `2F:36:A5:FF:0B:70:97:E6:F0:28:72:2A:DC:99:BC:14:9A:B8:64:7D:0D:83:DB:87:6B:BB:24:54:62:8F:F3:A3` |

تعرض الصفحة خيار **طلب إعادة ضبط مفتاح التحميل**. لم يُضغط الزر ولم يُرسل أي طلب بعد. إعادة الضبط المقصودة تخص مفتاح الرفع فقط ولا تستبدل مفتاح توقيع التطبيق الذي تحتفظ به Google.

بعد تأكيد المستخدم، أُعيد التحقق من صفحة `https://play.google.com/console/u/1/developers/5527675669406446893/app/4973046056164152935/keymanagement` وما زال زر **طلب إعادة ضبط مفتاح التحميل** ظاهراً، مع بقاء شهادة الرفع القديمة `86:DE...0F:7C` مسجلة. الخطوة التالية هي فتح نموذج الطلب ورفع شهادة المفتاح الجديد المعتمدة.

أظهر استخدام مفتاح `End` نهاية صفحة توقيع التطبيق عند قسم Digital Asset Links؛ زر **طلب إعادة ضبط مفتاح التحميل** يقع أعلى هذا الموضع داخل قسم شهادة مفتاح التحميل. لم يُفتح النموذج بعد.

تم إظهار قسم **إعادة ضبط مفتاح التحميل** في أسفل صفحة إدارة المفاتيح بعد الرجوع صفحة واحدة للأعلى. رابط طلب إعادة الضبط ظاهر وجاهز للفتح وفق تأكيد المستخدم؛ لم يُضغط بعد.

أظهر التحديث الحالي لصفحة إدارة المفاتيح زر **طلب إعادة ضبط مفتاح التحميل** بوضوح في قسم شهادة مفتاح الرفع، وأصبح قابلاً للفتح مباشرة. تأكيد المستخدم على تنفيذ العملية مسجل، والخطوة التالية فتح نموذج Google ثم مراجعة بياناته قبل الإرسال.

فُتح نموذج إعادة ضبط مفتاح التحميل، وتم اختيار السبب الصحيح **فقدتُ مفتاح التحميل**. يطلب النموذج شهادة عامة بصيغة PEM؛ لم يُرفع الملف ولم يُرسل الطلب بعد.

نموذج Google Play المفتوح يطلب: سبب إعادة الضبط (تم اختيار «فقدتُ مفتاح التحميل») وشهادة مفتاح الرفع بصيغة PEM. ملف HTML المحفوظ يؤكد وجود عنصر `key-file-picker-section` بالمعرّف `upload-certificate-file-picker` وزر «تحميل شهادة مفتاح التحميل» يتبعه حقل `<input hidden type="file">`. التمرير العادي لم يغيّر موضع النموذج؛ لم يُرفع الملف ولم يُرسل الطلب بعد.

لم يتغيّر موضع نافذة إعادة الضبط عند محاولة تمرير الحاوية، كما لم يظهر زر رفع الشهادة ضمن العناصر المرئية بعد تصغير العرض مرة واحدة. نموذج الطلب ما يزال مفتوحاً والسبب المحدد محفوظ؛ لم يُرفع الملف ولم يُرسل الطلب.

بعد تصغير العرض مرتين إضافيتين، بقي زر رفع شهادة المفتاح خارج قائمة العناصر المرئية رغم أن HTML يؤكد وجوده داخل النافذة المرئية (`pane-id=default--4`) باسم «تحميل شهادة مفتاح التحميل»، ويتبعه حقل ملف مخفي واحد. زر «طلب» ما يزال معطلاً، ولم يُرفع الملف أو يُرسل الطلب.

محاولة توجيه الرفع إلى فهرس تخميني رُفضت لأن الصفحة تغيّرت، ولم يُرفع أي ملف. بعد تحديث لقطة الصفحة بقيت العناصر المرئية مرقمة 1–16 فقط، وحقل الملف المخفي غير مفهرس؛ النموذج والسبب المحدد ما زالا مفتوحين وزر «طلب» معطلاً.

تم تركيز خيار «فقدتُ مفتاح التحميل» المحدد، ثم ضغط Tab مرة واحدة؛ ظل النموذج مفتوحاً دون تحميل ملف، وزر «طلب» ما يزال معطلاً. سيتم استخدام التنقل بلوحة المفاتيح للوصول إلى زر الرفع غير الظاهر في اللقطة.

بعد التنقل بلوحة المفاتيح أصبح زر رفع شهادة مفتاح التحميل ظاهراً ومفهرساً في النموذج: عنصر الغلاف 14 والزر الداخلي 15. لم يُرفع الملف بعد، وزر «طلب» ما يزال معطلاً.

محاولة رفع الملف إلى الزر الداخلي فشلت لأن العنصر زر إرسال بلا input تابع. تم بعدها الضغط على زر رفع شهادة PEM وفتح منتقي الملفات المحلي؛ لا يمكن لأدوات الصفحة التحكم بنافذة اختيار الملفات في جهاز المستخدم، ولم يُرسل الطلب بعد.

## حالة طلب إعادة تعيين Upload Key — 2026-07-19

أرسل المستخدم الطلب بعد رفع `upload_certificate.pem`. تعرض Google Play Console الآن رسالة: «هناك طلب في انتظار المراجعة لإعادة ضبط مفتاح التحميل لهذا التطبيق»، ما يؤكد أن الطلب أُرسل بنجاح ولم يعد مجرد مسودة. البصمة الجديدة المطلوبة هي `8D:10:76:F8:84:31:F2:6C:B5:0E:4B:50:F1:AE:9A:3A:F7:9C:34:A5`. لا يزال مفتاح توقيع التطبيق لدى Google Play دون تغيير (`7B:32:12:52:46:60:9E:C1:8A:ED:FC:4B:0E:BA:6E:5C:A3:13:CB:51`). يلزم انتظار موافقة Google قبل رفع AAB موقّع بالمفتاح الجديد.

البريد المتعلق باسترداد حساب Google منفصل عن طلب إعادة تعيين مفتاح الرفع، ولا يدل على رفض طلب Play Console.

## موافقة Google وموعد التفعيل

وصل بريد Google Play يؤكد قبول إعادة ضبط مفتاح الرفع. سيصبح المفتاح الجديد صالحاً في `2026-07-20 23:55 UTC`، الموافق `2026-07-21 02:55 GMT+3`. البصمات التي أكدتها Google:

- MD5: `5C:01:82:E6:02:F2:ED:50:F2:E2:AF:FC:9E:06:87:48`
- SHA-1: `8D:10:76:F8:84:31:F2:6C:B5:0E:4B:50:F1:AE:9A:3A:F7:9C:34:A5`

لا يمكن رفع APK أو AAB جديد قبل وقت التفعيل. يمكن تجهيز EAS والبناء مسبقاً، لكن يجب تأجيل رفع الحزمة إلى Google Play حتى يصبح المفتاح صالحاً.

## ربط المفتاح الجديد بـ EAS — نموذج إنشاء البيانات

تم فتح نموذج `Create Build Credentials` للحزمة `io.awafiyat.health` وتسميته `Google Play Upload Key 2026` مع تحديده كإعداد افتراضي. اختار المستخدم الملف `awafiyat-upload-key.jks` بنجاح، وأُدخلت كلمة مرور مخزن المفاتيح والاسم المستعار `awafiyat-upload-2026` دون حفظ أي أسرار في المستودع. بقي إدخال كلمة مرور المفتاح، ثم حفظ التكوين والتحقق من بصمة SHA-1 التي يعرضها EAS.

### نتيجة حفظ بيانات اعتماد EAS

حُفظت مجموعة جديدة باسم `Google Play Upload Key 2026` في مشروع `@zakariasalah/awafiyat` للحزمة `io.awafiyat.health`. أظهر EAS الاسم المستعار `awafiyat-upload-2026` والبصمات المختصرة التالية، وهي مطابقة للقيم الكاملة المتحقَّق منها محلياً ولشهادة Google Play:

| البصمة | قيمة EAS المختصرة | النتيجة |
|---|---|---|
| MD5 | `5C:01...87:48` | مطابقة |
| SHA-1 | `8D:10...34:A5` | مطابقة |
| SHA-256 | `2F:36...F3:A3` | مطابقة |

تبيّن بعد الحفظ أن المجموعة القديمة `Build Credentials jV3TkDMudc` ما زالت تحمل شارة `Default`، بينما المجموعة الجديدة موجودة لكنها لم تصبح افتراضية بعد. يلزم تعيين `Google Play Upload Key 2026` كافتراضية قبل إطلاق بناء الإنتاج.

فُحصت قائمة إجراءات مخزن المفتاح الجديد. الخيارات المتاحة هي `Download` و`Edit` و`Delete` فقط، ونموذج `Edit` مخصص لاستبدال ملف مخزن المفتاح وبياناته وليس لتعيين مجموعة Build Credentials افتراضية؛ أُغلق النموذج دون حفظ أي تعديل. سيُستخدم بدلاً منه زر إجراءات **مجموعة بيانات الاعتماد** المجاور لمحدد المجموعات.

فُتح نموذج `Edit Build Credentials` للمجموعة `Google Play Upload Key 2026`، وظهر خيار `Use as Default` غير محدد. تم تفعيله ثم ضغط `Save` وفق موافقة المستخدم. أثناء تنفيذ الحفظ ظهر مؤشر معالجة، لذلك يلزم انتظار اكتمال العملية ثم إعادة التحقق من شارة `Default` على المجموعة الجديدة وعدم الاعتماد على الحالة الانتقالية للواجهة.

### التحقق النهائي من الإعداد الافتراضي

بعد اكتمال الحفظ وإعادة تحميل صفحة EAS بالكامل، ظهرت مجموعة `Google Play Upload Key 2026` محددة تلقائياً ومعها شارة الحالة الافتراضية، وعرضت مخزن المفتاح `awafiyat-upload-2026` ببصمات `5C:01...87:48` و`8D:10...34:A5` و`2F:36...F3:A3`. وبذلك انتقل إعداد Android الافتراضي فعلياً إلى المفتاح الجديد المطابق لشهادة Google Play. لم يُحذف مخزن المفتاح السابق، ولم يبدأ أي بناء أو إرسال إلى Google Play.
